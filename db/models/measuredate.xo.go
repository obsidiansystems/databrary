// Package xo_models contains the types for schema 'public'.
package xo_models

// GENERATED BY XO. DO NOT EDIT.

import (
	"errors"
	"time"
)

// MeasureDate represents a row from '"public"."measure_date"'.
type MeasureDate struct {
	Record int64     `json:"record"` // record
	Metric int64     `json:"metric"` // metric
	Datum  time.Time `json:"datum"`  // datum

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the MeasureDate exists in the database.
func (md *MeasureDate) Exists() bool {
	return md._exists
}

// Deleted provides information if the MeasureDate has been deleted from the database.
func (md *MeasureDate) Deleted() bool {
	return md._deleted
}

// Insert inserts the MeasureDate to the database.
func (md *MeasureDate) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if md._exists {
		return errors.New("insert failed: already exists")
	}

	// sql insert query, primary key must be provided
	const sqlstr = `INSERT INTO "public"."measure_date" (` +
		`"record", "metric", "datum"` +
		`) VALUES (` +
		`$1, $2, $3` +
		`)`

	// run query
	XOLog(sqlstr, md.Record, md.Metric, md.Datum)
	err = db.QueryRow(sqlstr, md.Record, md.Metric, md.Datum).Scan(&md.Metric)
	if err != nil {
		return err
	}

	// set existence
	md._exists = true

	return nil
}

// Update updates the MeasureDate in the database.
func (md *MeasureDate) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !md._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if md._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE "public"."measure_date" SET (` +
		`"record", "datum"` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE "metric" = $3`

	// run query
	XOLog(sqlstr, md.Record, md.Datum, md.Metric)
	_, err = db.Exec(sqlstr, md.Record, md.Datum, md.Metric)
	return err
}

// Save saves the MeasureDate to the database.
func (md *MeasureDate) Save(db XODB) error {
	if md.Exists() {
		return md.Update(db)
	}

	return md.Insert(db)
}

// Upsert performs an upsert for MeasureDate.
//
// NOTE: PostgreSQL 9.5+ only
func (md *MeasureDate) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if md._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO "public"."measure_date" (` +
		`"record", "metric", "datum"` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT ("metric") DO UPDATE SET (` +
		`"record", "metric", "datum"` +
		`) = (` +
		`EXCLUDED."record", EXCLUDED."metric", EXCLUDED."datum"` +
		`)`

	// run query
	XOLog(sqlstr, md.Record, md.Metric, md.Datum)
	_, err = db.Exec(sqlstr, md.Record, md.Metric, md.Datum)
	if err != nil {
		return err
	}

	// set existence
	md._exists = true

	return nil
}

// Delete deletes the MeasureDate from the database.
func (md *MeasureDate) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !md._exists {
		return nil
	}

	// if deleted, bail
	if md._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM "public"."measure_date" WHERE "metric" = $1`

	// run query
	XOLog(sqlstr, md.Metric)
	_, err = db.Exec(sqlstr, md.Metric)
	if err != nil {
		return err
	}

	// set deleted
	md._deleted = true

	return nil
}

// Metric returns the Metric associated with the MeasureDate's Metric (metric).
//
// Generated from foreign key 'measure_date_metric_fkey'.
func (md *MeasureDate) MetricByMetricID(db XODB) (*Metric, error) {
	return MetricByID(db, md.Metric)
}

// Record returns the Record associated with the MeasureDate's Record (record).
//
// Generated from foreign key 'measure_date_record_fkey'.
func (md *MeasureDate) RecordByRecordID(db XODB) (*Record, error) {
	return RecordByID(db, md.Record)
}

// MeasureDateByRecordMetric retrieves a row from '"public"."measure_date"' as a MeasureDate.
//
// Generated from index 'measure_date_pkey'.
func MeasureDateByRecordMetric(db XODB, record int64, metric int64) (*MeasureDate, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`"record", "metric", "datum" ` +
		`FROM "public"."measure_date" ` +
		`WHERE "record" = $1 AND "metric" = $2`

	// run query
	XOLog(sqlstr, record, metric)
	md := MeasureDate{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, record, metric).Scan(&md.Record, &md.Metric, &md.Datum)
	if err != nil {
		return nil, err
	}

	return &md, nil
}
