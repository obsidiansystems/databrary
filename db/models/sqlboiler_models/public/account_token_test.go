// This file is generated by SQLBoiler (https://github.com/databrary/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// EDIT AT YOUR OWN RISK

package public

import (
	"bytes"
	"github.com/databrary/sqlboiler/boil"
	"github.com/databrary/sqlboiler/randomize"
	"github.com/databrary/sqlboiler/strmangle"
	"github.com/pmezard/go-difflib/difflib"
	"os"
	"os/exec"
	"reflect"
	"sort"
	"strings"
	"testing"
)

func testAccountTokens(t *testing.T) {
	t.Parallel()

	query := AccountTokens(nil)

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testAccountTokensLive(t *testing.T) {
	all, err := AccountTokens(dbMain.liveDbConn).All()
	if err != nil {
		t.Fatalf("failed to get all AccountTokens err: ", err)
	}
	tx, err := dbMain.liveTestDbConn.Begin()
	if err != nil {
		t.Fatalf("failed to begin transaction: ", err)
	}
	for _, v := range all {
		err := v.Insert(tx)
		if err != nil {
			t.Fatalf("failed to failed to insert %s because of %s", v, err)
		}

	}
	err = tx.Commit()
	if err != nil {
		t.Fatalf("failed to commit transaction: ", err)
	}
	bf := &bytes.Buffer{}
	dumpCmd := exec.Command("psql", `-c "COPY (SELECT * FROM account_token) TO STDOUT" -d `, dbMain.DbName)
	dumpCmd.Env = append(os.Environ(), dbMain.pgEnv()...)
	dumpCmd.Stdout = bf
	err = dumpCmd.Start()
	if err != nil {
		t.Fatalf("failed to start dump from live db because of %s", err)
	}
	dumpCmd.Wait()
	if err != nil {
		t.Fatalf("failed to wait dump from live db because of %s", err)
	}
	bg := &bytes.Buffer{}
	dumpCmd = exec.Command("psql", `-c "COPY (SELECT * FROM account_token) TO STDOUT" -d `, dbMain.LiveTestDBName)
	dumpCmd.Env = append(os.Environ(), dbMain.pgEnv()...)
	dumpCmd.Stdout = bg
	err = dumpCmd.Start()
	if err != nil {
		t.Fatalf("failed to start dump from test db because of %s", err)
	}
	dumpCmd.Wait()
	if err != nil {
		t.Fatalf("failed to wait dump from test db because of %s", err)
	}
	bfslice := sort.StringSlice(difflib.SplitLines(bf.String()))
	gfslice := sort.StringSlice(difflib.SplitLines(bg.String()))
	bfslice.Sort()
	gfslice.Sort()
	diff := difflib.ContextDiff{
		A:        bfslice,
		B:        gfslice,
		FromFile: "databrary",
		ToFile:   "test",
		Context:  1,
	}
	result, _ := difflib.GetContextDiffString(diff)
	if len(result) > 0 {
		t.Fatalf("AccountTokensLive failed but it's probably trivial: %s", strings.Replace(result, "\t", " ", -1))
	}

}

func testAccountTokensDelete(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = accountToken.Delete(tx); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAccountTokensQueryDeleteAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = AccountTokens(tx).DeleteAll(); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAccountTokensSliceDeleteAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := AccountTokenSlice{accountToken}

	if err = slice.DeleteAll(tx); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAccountTokensExists(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	e, err := AccountTokenExists(tx, accountToken.Token)
	if err != nil {
		t.Errorf("Unable to check if AccountToken exists: %s", err)
	}
	if !e {
		t.Errorf("Expected AccountTokenExistsG to return true, but got false.")
	}
}

func testAccountTokensFind(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	accountTokenFound, err := FindAccountToken(tx, accountToken.Token)
	if err != nil {
		t.Error(err)
	}

	if accountTokenFound == nil {
		t.Error("want a record, got nil")
	}
}

func testAccountTokensBind(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = AccountTokens(tx).Bind(accountToken); err != nil {
		t.Error(err)
	}
}

func testAccountTokensOne(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	if x, err := AccountTokens(tx).One(); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testAccountTokensAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountTokenOne := &AccountToken{}
	accountTokenTwo := &AccountToken{}
	if err = randomize.Struct(seed, accountTokenOne, accountTokenDBTypes, false, accountTokenColumnsWithDefault...); err != nil {

		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}
	if err = randomize.Struct(seed, accountTokenTwo, accountTokenDBTypes, false, accountTokenColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountTokenOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = accountTokenTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := AccountTokens(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testAccountTokensCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountTokenOne := &AccountToken{}
	accountTokenTwo := &AccountToken{}
	if err = randomize.Struct(seed, accountTokenOne, accountTokenDBTypes, false, accountTokenColumnsWithDefault...); err != nil {

		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}
	if err = randomize.Struct(seed, accountTokenTwo, accountTokenDBTypes, false, accountTokenColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountTokenOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = accountTokenTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func accountTokenBeforeInsertHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenAfterInsertHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenAfterSelectHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenBeforeUpdateHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenAfterUpdateHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenBeforeDeleteHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenAfterDeleteHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenBeforeUpsertHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func accountTokenAfterUpsertHook(e boil.Executor, o *AccountToken) error {
	*o = AccountToken{}
	return nil
}

func testAccountTokensHooks(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	empty := &AccountToken{}

	AddAccountTokenHook(boil.BeforeInsertHook, accountTokenBeforeInsertHook)
	if err = accountToken.doBeforeInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenBeforeInsertHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.AfterInsertHook, accountTokenAfterInsertHook)
	if err = accountToken.doAfterInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenAfterInsertHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.AfterSelectHook, accountTokenAfterSelectHook)
	if err = accountToken.doAfterSelectHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenAfterSelectHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.BeforeUpdateHook, accountTokenBeforeUpdateHook)
	if err = accountToken.doBeforeUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenBeforeUpdateHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.AfterUpdateHook, accountTokenAfterUpdateHook)
	if err = accountToken.doAfterUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenAfterUpdateHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.BeforeDeleteHook, accountTokenBeforeDeleteHook)
	if err = accountToken.doBeforeDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenBeforeDeleteHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.AfterDeleteHook, accountTokenAfterDeleteHook)
	if err = accountToken.doAfterDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenAfterDeleteHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.BeforeUpsertHook, accountTokenBeforeUpsertHook)
	if err = accountToken.doBeforeUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenBeforeUpsertHooks = []AccountTokenHook{}

	AddAccountTokenHook(boil.AfterUpsertHook, accountTokenAfterUpsertHook)
	if err = accountToken.doAfterUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(accountToken, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", accountToken)
	}
	accountTokenAfterUpsertHooks = []AccountTokenHook{}
}
func testAccountTokensInsert(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAccountTokensInsertWhitelist(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx, accountTokenColumns...); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAccountTokenToOneAccountUsingAccount(t *testing.T) {
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	seed := randomize.NewSeed()

	var foreign Account
	var local AccountToken

	foreignBlacklist := accountColumnsWithDefault
	if err := randomize.Struct(seed, &foreign, accountDBTypes, true, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Account struct: %s", err)
	}
	localBlacklist := accountTokenColumnsWithDefault
	if err := randomize.Struct(seed, &local, accountTokenDBTypes, true, localBlacklist...); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	if err := foreign.Insert(tx); err != nil {
		t.Fatal(err)
	}

	local.Account = foreign.ID
	if err := local.Insert(tx); err != nil {
		t.Fatal(err)
	}

	check, err := local.AccountByFk(tx).One()
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := AccountTokenSlice{&local}
	if err = local.L.LoadAccount(tx, false, &slice); err != nil {
		t.Fatal(err)
	}
	if local.R.Account == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Account = nil
	if err = local.L.LoadAccount(tx, true, &local); err != nil {
		t.Fatal(err)
	}
	if local.R.Account == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testAccountTokenToOneSetOpAccountUsingAccount(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	seed := randomize.NewSeed()

	var a AccountToken
	var b, c Account

	foreignBlacklist := strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)
	if err := randomize.Struct(seed, &b, accountDBTypes, false, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Account struct: %s", err)
	}
	if err := randomize.Struct(seed, &c, accountDBTypes, false, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Account struct: %s", err)
	}
	localBlacklist := strmangle.SetComplement(accountTokenPrimaryKeyColumns, accountTokenColumnsWithoutDefault)
	if err := randomize.Struct(seed, &a, accountTokenDBTypes, false, localBlacklist...); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Account{&b, &c} {
		err = a.SetAccount(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Account != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.AccountTokens[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.Account != x.ID {
			t.Error("foreign key was wrong value", a.Account)
		}

		zero := reflect.Zero(reflect.TypeOf(a.Account))
		reflect.Indirect(reflect.ValueOf(&a.Account)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.Account != x.ID {
			t.Error("foreign key was wrong value", a.Account, x.ID)
		}
	}
}

func testAccountTokensReload(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = accountToken.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testAccountTokensReloadAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := AccountTokenSlice{accountToken}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testAccountTokensSelect(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := AccountTokens(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	accountTokenDBTypes = map[string]string{`Account`: `integer`, `Expires`: `timestamp with time zone`, `Token`: `character varying`}
	_                   = bytes.MinRead
)

func testAccountTokensUpdate(t *testing.T) {
	t.Parallel()

	if len(accountTokenColumns) == len(accountTokenPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	blacklist := accountTokenColumnsWithDefault

	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true, blacklist...); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	if err = accountToken.Update(tx); err != nil {
		t.Error(err)
	}
}

func testAccountTokensSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(accountTokenColumns) == len(accountTokenPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	blacklist := accountTokenPrimaryKeyColumns

	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true, blacklist...); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(accountTokenColumns, accountTokenPrimaryKeyColumns) {
		fields = accountTokenColumns
	} else {
		fields = strmangle.SetComplement(
			accountTokenColumns,
			accountTokenPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(accountToken))
	updateMap := M{}
	for _, col := range fields {
		updateMap[col] = value.FieldByName(strmangle.TitleCase(col)).Interface()
	}

	slice := AccountTokenSlice{accountToken}
	if err = slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	}
}

func testAccountTokensUpsert(t *testing.T) {
	t.Parallel()

	if len(accountTokenColumns) == len(accountTokenPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	var err error
	seed := randomize.NewSeed()
	accountToken := &AccountToken{}
	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = accountToken.Upsert(tx, false, nil, nil); err != nil {
		t.Errorf("Unable to upsert AccountToken: %s", err)
	}

	count, err := AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	blacklist := accountTokenPrimaryKeyColumns

	if err = randomize.Struct(seed, accountToken, accountTokenDBTypes, false, blacklist...); err != nil {
		t.Errorf("Unable to randomize AccountToken struct: %s", err)
	}

	if err = accountToken.Upsert(tx, true, nil, nil); err != nil {
		t.Errorf("Unable to upsert AccountToken: %s", err)
	}

	count, err = AccountTokens(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
