// This file is generated by SQLBoiler (https://github.com/databrary/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// EDIT AT YOUR OWN RISK

package public

import (
	"bytes"
	"database/sql"
	"fmt"
	"github.com/databrary/databrary/db/models/custom_types"
	"github.com/databrary/sqlboiler/boil"
	"github.com/databrary/sqlboiler/queries"
	"github.com/databrary/sqlboiler/queries/qm"
	"github.com/databrary/sqlboiler/strmangle"
	"github.com/pkg/errors"
	"reflect"
	"strings"
	"sync"
	"time"
)

// SlotRecord is an object representing the database table.
type SlotRecord struct {
	Container int                  `db:"container" json:"slotRecord_container"`
	Segment   custom_types.Segment `db:"segment" json:"slotRecord_segment"`
	Record    int                  `db:"record" json:"slotRecord_record"`

	R *slotRecordR `db:"-" json:"-"`
	L slotRecordL  `db:"-" json:"-"`
}

// slotRecordR is where relationships are stored.
type slotRecordR struct {
	Container *Container
	Record    *Record
}

// slotRecordL is where Load methods for each relationship are stored.
type slotRecordL struct{}

var (
	slotRecordColumns               = []string{"container", "segment", "record"}
	slotRecordColumnsWithoutDefault = []string{"container", "segment", "record"}
	slotRecordColumnsWithDefault    = []string{}
	slotRecordColumnsWithCustom     = []string{"segment"}

	slotRecordPrimaryKeyColumns = []string{"container", "segment", "record"}
)

type (
	// SlotRecordSlice is an alias for a slice of pointers to SlotRecord.
	// This should generally be used opposed to []SlotRecord.
	SlotRecordSlice []*SlotRecord
	// SlotRecordHook is the signature for custom SlotRecord hook methods
	SlotRecordHook func(boil.Executor, *SlotRecord) error

	slotRecordQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	slotRecordType    = reflect.TypeOf(&SlotRecord{})
	slotRecordMapping = queries.MakeStructMapping(slotRecordType)

	slotRecordPrimaryKeyMapping, _ = queries.BindMapping(slotRecordType, slotRecordMapping, slotRecordPrimaryKeyColumns)

	slotRecordInsertCacheMut sync.RWMutex
	slotRecordInsertCache    = make(map[string]insertCache)
	slotRecordUpdateCacheMut sync.RWMutex
	slotRecordUpdateCache    = make(map[string]updateCache)
	slotRecordUpsertCacheMut sync.RWMutex
	slotRecordUpsertCache    = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var slotRecordBeforeInsertHooks []SlotRecordHook
var slotRecordBeforeUpdateHooks []SlotRecordHook
var slotRecordBeforeDeleteHooks []SlotRecordHook
var slotRecordBeforeUpsertHooks []SlotRecordHook

var slotRecordAfterInsertHooks []SlotRecordHook
var slotRecordAfterSelectHooks []SlotRecordHook
var slotRecordAfterUpdateHooks []SlotRecordHook
var slotRecordAfterDeleteHooks []SlotRecordHook
var slotRecordAfterUpsertHooks []SlotRecordHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SlotRecord) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SlotRecord) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SlotRecord) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SlotRecord) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SlotRecord) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SlotRecord) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SlotRecord) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SlotRecord) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SlotRecord) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range slotRecordAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSlotRecordHook registers your hook function for all future operations.
func AddSlotRecordHook(hookPoint boil.HookPoint, slotRecordHook SlotRecordHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		slotRecordBeforeInsertHooks = append(slotRecordBeforeInsertHooks, slotRecordHook)
	case boil.BeforeUpdateHook:
		slotRecordBeforeUpdateHooks = append(slotRecordBeforeUpdateHooks, slotRecordHook)
	case boil.BeforeDeleteHook:
		slotRecordBeforeDeleteHooks = append(slotRecordBeforeDeleteHooks, slotRecordHook)
	case boil.BeforeUpsertHook:
		slotRecordBeforeUpsertHooks = append(slotRecordBeforeUpsertHooks, slotRecordHook)
	case boil.AfterInsertHook:
		slotRecordAfterInsertHooks = append(slotRecordAfterInsertHooks, slotRecordHook)
	case boil.AfterSelectHook:
		slotRecordAfterSelectHooks = append(slotRecordAfterSelectHooks, slotRecordHook)
	case boil.AfterUpdateHook:
		slotRecordAfterUpdateHooks = append(slotRecordAfterUpdateHooks, slotRecordHook)
	case boil.AfterDeleteHook:
		slotRecordAfterDeleteHooks = append(slotRecordAfterDeleteHooks, slotRecordHook)
	case boil.AfterUpsertHook:
		slotRecordAfterUpsertHooks = append(slotRecordAfterUpsertHooks, slotRecordHook)
	}
}

// OneP returns a single slotRecord record from the query, and panics on error.
func (q slotRecordQuery) OneP() *SlotRecord {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single slotRecord record from the query.
func (q slotRecordQuery) One() (*SlotRecord, error) {
	o := &SlotRecord{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "public: failed to execute a one query for slot_record")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all SlotRecord records from the query, and panics on error.
func (q slotRecordQuery) AllP() SlotRecordSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SlotRecord records from the query.
func (q slotRecordQuery) All() (SlotRecordSlice, error) {
	var o SlotRecordSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "public: failed to assign all query results to SlotRecord slice")
	}

	if len(slotRecordAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all SlotRecord records in the query, and panics on error.
func (q slotRecordQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SlotRecord records in the query.
func (q slotRecordQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "public: failed to count slot_record rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q slotRecordQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q slotRecordQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "public: failed to check if slot_record exists")
	}

	return count > 0, nil
}

// ContainerG pointed to by the foreign key.
func (o *SlotRecord) ContainerG(mods ...qm.QueryMod) containerQuery {
	return o.ContainerByFk(boil.GetDB(), mods...)
}

// Container pointed to by the foreign key.
func (o *SlotRecord) ContainerByFk(exec boil.Executor, mods ...qm.QueryMod) containerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.Container),
	}

	queryMods = append(queryMods, mods...)

	query := Containers(exec, queryMods...)
	queries.SetFrom(query.Query, "\"container\"")

	return query
}

// RecordG pointed to by the foreign key.
func (o *SlotRecord) RecordG(mods ...qm.QueryMod) recordQuery {
	return o.RecordByFk(boil.GetDB(), mods...)
}

// Record pointed to by the foreign key.
func (o *SlotRecord) RecordByFk(exec boil.Executor, mods ...qm.QueryMod) recordQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.Record),
	}

	queryMods = append(queryMods, mods...)

	query := Records(exec, queryMods...)
	queries.SetFrom(query.Query, "\"record\"")

	return query
}

// LoadContainer allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (slotRecordL) LoadContainer(e boil.Executor, singular bool, maybeSlotRecord interface{}) error {
	var slice []*SlotRecord
	var object *SlotRecord

	count := 1
	if singular {
		object = maybeSlotRecord.(*SlotRecord)
	} else {
		slice = *maybeSlotRecord.(*SlotRecordSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &slotRecordR{}
		}
		args[0] = object.Container
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &slotRecordR{}
			}
			args[i] = obj.Container
		}
	}

	query := fmt.Sprintf(
		"select * from \"container\" where \"id\" in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Container")
	}
	defer results.Close()

	var resultSlice []*Container
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Container")
	}

	if len(slotRecordAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Container = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Container == foreign.ID {
				local.R.Container = foreign
				break
			}
		}
	}

	return nil
}

// LoadRecord allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (slotRecordL) LoadRecord(e boil.Executor, singular bool, maybeSlotRecord interface{}) error {
	var slice []*SlotRecord
	var object *SlotRecord

	count := 1
	if singular {
		object = maybeSlotRecord.(*SlotRecord)
	} else {
		slice = *maybeSlotRecord.(*SlotRecordSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &slotRecordR{}
		}
		args[0] = object.Record
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &slotRecordR{}
			}
			args[i] = obj.Record
		}
	}

	query := fmt.Sprintf(
		"select * from \"record\" where \"id\" in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Record")
	}
	defer results.Close()

	var resultSlice []*Record
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Record")
	}

	if len(slotRecordAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Record = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Record == foreign.ID {
				local.R.Record = foreign
				break
			}
		}
	}

	return nil
}

// SetContainerG of the slot_record to the related item.
// Sets o.R.Container to related.
// Adds o to related.R.SlotRecords.
// Uses the global database handle.
func (o *SlotRecord) SetContainerG(insert bool, related *Container) error {
	return o.SetContainer(boil.GetDB(), insert, related)
}

// SetContainerP of the slot_record to the related item.
// Sets o.R.Container to related.
// Adds o to related.R.SlotRecords.
// Panics on error.
func (o *SlotRecord) SetContainerP(exec boil.Executor, insert bool, related *Container) {
	if err := o.SetContainer(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetContainerGP of the slot_record to the related item.
// Sets o.R.Container to related.
// Adds o to related.R.SlotRecords.
// Uses the global database handle and panics on error.
func (o *SlotRecord) SetContainerGP(insert bool, related *Container) {
	if err := o.SetContainer(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetContainer of the slot_record to the related item.
// Sets o.R.Container to related.
// Adds o to related.R.SlotRecords.
func (o *SlotRecord) SetContainer(exec boil.Executor, insert bool, related *Container) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"slot_record\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"container"}),
		strmangle.WhereClause("\"", "\"", 2, slotRecordPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.Container, o.Segment, o.Record}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Container = related.ID

	if o.R == nil {
		o.R = &slotRecordR{
			Container: related,
		}
	} else {
		o.R.Container = related
	}

	if related.R == nil {
		related.R = &containerR{
			SlotRecords: SlotRecordSlice{o},
		}
	} else {
		related.R.SlotRecords = append(related.R.SlotRecords, o)
	}

	return nil
}

// SetRecordG of the slot_record to the related item.
// Sets o.R.Record to related.
// Adds o to related.R.SlotRecords.
// Uses the global database handle.
func (o *SlotRecord) SetRecordG(insert bool, related *Record) error {
	return o.SetRecord(boil.GetDB(), insert, related)
}

// SetRecordP of the slot_record to the related item.
// Sets o.R.Record to related.
// Adds o to related.R.SlotRecords.
// Panics on error.
func (o *SlotRecord) SetRecordP(exec boil.Executor, insert bool, related *Record) {
	if err := o.SetRecord(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetRecordGP of the slot_record to the related item.
// Sets o.R.Record to related.
// Adds o to related.R.SlotRecords.
// Uses the global database handle and panics on error.
func (o *SlotRecord) SetRecordGP(insert bool, related *Record) {
	if err := o.SetRecord(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetRecord of the slot_record to the related item.
// Sets o.R.Record to related.
// Adds o to related.R.SlotRecords.
func (o *SlotRecord) SetRecord(exec boil.Executor, insert bool, related *Record) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"slot_record\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"record"}),
		strmangle.WhereClause("\"", "\"", 2, slotRecordPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.Container, o.Segment, o.Record}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Record = related.ID

	if o.R == nil {
		o.R = &slotRecordR{
			Record: related,
		}
	} else {
		o.R.Record = related
	}

	if related.R == nil {
		related.R = &recordR{
			SlotRecords: SlotRecordSlice{o},
		}
	} else {
		related.R.SlotRecords = append(related.R.SlotRecords, o)
	}

	return nil
}

// SlotRecordsG retrieves all records.
func SlotRecordsG(mods ...qm.QueryMod) slotRecordQuery {
	return SlotRecords(boil.GetDB(), mods...)
}

// SlotRecords retrieves all the records using an executor.
func SlotRecords(exec boil.Executor, mods ...qm.QueryMod) slotRecordQuery {
	mods = append(mods, qm.From("\"slot_record\""))
	return slotRecordQuery{NewQuery(exec, mods...)}
}

// FindSlotRecordG retrieves a single record by ID.
func FindSlotRecordG(container int, segment custom_types.Segment, record int, selectCols ...string) (*SlotRecord, error) {
	return FindSlotRecord(boil.GetDB(), container, segment, record, selectCols...)
}

// FindSlotRecordGP retrieves a single record by ID, and panics on error.
func FindSlotRecordGP(container int, segment custom_types.Segment, record int, selectCols ...string) *SlotRecord {
	retobj, err := FindSlotRecord(boil.GetDB(), container, segment, record, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSlotRecord retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSlotRecord(exec boil.Executor, container int, segment custom_types.Segment, record int, selectCols ...string) (*SlotRecord, error) {
	slotRecordObj := &SlotRecord{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"slot_record\" where \"container\"=$1 AND \"segment\"=$2 AND \"record\"=$3", sel,
	)

	q := queries.Raw(exec, query, container, segment, record)

	err := q.Bind(slotRecordObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "public: unable to select from slot_record")
	}

	return slotRecordObj, nil
}

// FindSlotRecordP retrieves a single record by ID with an executor, and panics on error.
func FindSlotRecordP(exec boil.Executor, container int, segment custom_types.Segment, record int, selectCols ...string) *SlotRecord {
	retobj, err := FindSlotRecord(exec, container, segment, record, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SlotRecord) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SlotRecord) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SlotRecord) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *SlotRecord) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("public: no slot_record provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(slotRecordColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	slotRecordInsertCacheMut.RLock()
	cache, cached := slotRecordInsertCache[key]
	slotRecordInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			slotRecordColumns,
			slotRecordColumnsWithDefault,
			slotRecordColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(slotRecordType, slotRecordMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(slotRecordType, slotRecordMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"slot_record\" (\"%s\") VALUES (%s)", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"slot_record\" DEFAULT VALUES"
		}

		if len(cache.retMapping) != 0 {
			cache.query += fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "public: unable to insert into slot_record")
	}

	if !cached {
		slotRecordInsertCacheMut.Lock()
		slotRecordInsertCache[key] = cache
		slotRecordInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single SlotRecord record. See Update for
// whitelist behavior description.
func (o *SlotRecord) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single SlotRecord record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *SlotRecord) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the SlotRecord, and panics on error.
// See Update for whitelist behavior description.
func (o *SlotRecord) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the SlotRecord.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *SlotRecord) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	slotRecordUpdateCacheMut.RLock()
	cache, cached := slotRecordUpdateCache[key]
	slotRecordUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(slotRecordColumns, slotRecordPrimaryKeyColumns, whitelist)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("public: unable to update slot_record, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"slot_record\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, slotRecordPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(slotRecordType, slotRecordMapping, append(wl, slotRecordPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "public: unable to update slot_record row")
	}

	if !cached {
		slotRecordUpdateCacheMut.Lock()
		slotRecordUpdateCache[key] = cache
		slotRecordUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q slotRecordQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q slotRecordQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "public: unable to update all for slot_record")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SlotRecordSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SlotRecordSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SlotRecordSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SlotRecordSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("public: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), slotRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"UPDATE \"slot_record\" SET %s WHERE (\"container\",\"segment\",\"record\") IN (%s)",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(slotRecordPrimaryKeyColumns), len(colNames)+1, len(slotRecordPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to update all in slotRecord slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SlotRecord) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SlotRecord) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SlotRecord) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *SlotRecord) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("public: no slot_record provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(slotRecordColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	slotRecordUpsertCacheMut.RLock()
	cache, cached := slotRecordUpsertCache[key]
	slotRecordUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			slotRecordColumns,
			slotRecordColumnsWithDefault,
			slotRecordColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			slotRecordColumns,
			slotRecordPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("public: unable to upsert slot_record, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(slotRecordPrimaryKeyColumns))
			copy(conflict, slotRecordPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"slot_record\"", updateOnConflict, ret, update, conflict, whitelist)

		cache.valueMapping, err = queries.BindMapping(slotRecordType, slotRecordMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(slotRecordType, slotRecordMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "public: unable to upsert slot_record")
	}

	if !cached {
		slotRecordUpsertCacheMut.Lock()
		slotRecordUpsertCache[key] = cache
		slotRecordUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single SlotRecord record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SlotRecord) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single SlotRecord record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SlotRecord) DeleteG() error {
	if o == nil {
		return errors.New("public: no SlotRecord provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single SlotRecord record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SlotRecord) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single SlotRecord record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SlotRecord) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("public: no SlotRecord provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), slotRecordPrimaryKeyMapping)
	query := "DELETE FROM \"slot_record\" WHERE \"container\"=$1 AND \"segment\"=$2 AND \"record\"=$3"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to delete from slot_record")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q slotRecordQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q slotRecordQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("public: no slotRecordQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "public: unable to delete all from slot_record")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SlotRecordSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SlotRecordSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("public: no SlotRecord slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SlotRecordSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SlotRecordSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("public: no SlotRecord slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(slotRecordBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), slotRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"DELETE FROM \"slot_record\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, slotRecordPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(slotRecordPrimaryKeyColumns), 1, len(slotRecordPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to delete all from slotRecord slice")
	}

	if len(slotRecordAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SlotRecord) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SlotRecord) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SlotRecord) ReloadG() error {
	if o == nil {
		return errors.New("public: no SlotRecord provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SlotRecord) Reload(exec boil.Executor) error {
	ret, err := FindSlotRecord(exec, o.Container, o.Segment, o.Record)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SlotRecordSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SlotRecordSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SlotRecordSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("public: empty SlotRecordSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SlotRecordSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slotRecords := SlotRecordSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), slotRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"SELECT \"slot_record\".* FROM \"slot_record\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, slotRecordPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(slotRecordPrimaryKeyColumns), 1, len(slotRecordPrimaryKeyColumns)),
	)

	q := queries.Raw(exec, query, args...)

	err := q.Bind(&slotRecords)
	if err != nil {
		return errors.Wrap(err, "public: unable to reload all in SlotRecordSlice")
	}

	*o = slotRecords

	return nil
}

// SlotRecordExists checks if the SlotRecord row exists.
func SlotRecordExists(exec boil.Executor, container int, segment custom_types.Segment, record int) (bool, error) {
	var exists bool

	query := "select exists(select 1 from \"slot_record\" where \"container\"=$1 AND \"segment\"=$2 AND \"record\"=$3 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, container, segment, record)
	}

	row := exec.QueryRow(query, container, segment, record)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "public: unable to check if slot_record exists")
	}

	return exists, nil
}

// SlotRecordExistsG checks if the SlotRecord row exists.
func SlotRecordExistsG(container int, segment custom_types.Segment, record int) (bool, error) {
	return SlotRecordExists(boil.GetDB(), container, segment, record)
}

// SlotRecordExistsGP checks if the SlotRecord row exists. Panics on error.
func SlotRecordExistsGP(container int, segment custom_types.Segment, record int) bool {
	e, err := SlotRecordExists(boil.GetDB(), container, segment, record)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SlotRecordExistsP checks if the SlotRecord row exists. Panics on error.
func SlotRecordExistsP(exec boil.Executor, container int, segment custom_types.Segment, record int) bool {
	e, err := SlotRecordExists(exec, container, segment, record)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
