// This file is generated by SQLBoiler (https://github.com/databrary/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// EDIT AT YOUR OWN RISK

package public

import (
	"bytes"
	"database/sql"
	"fmt"
	"github.com/databrary/sqlboiler/boil"
	"github.com/databrary/sqlboiler/queries"
	"github.com/databrary/sqlboiler/queries/qm"
	"github.com/databrary/sqlboiler/strmangle"
	"github.com/pkg/errors"
	"reflect"
	"strings"
	"sync"
	"time"
)

// PlayEvolutionsLock is an object representing the database table.
type PlayEvolutionsLock struct {
	Lock int `db:"lock" json:"playEvolutionsLock_lock"`

	R *playEvolutionsLockR `db:"-" json:"-"`
	L playEvolutionsLockL  `db:"-" json:"-"`
}

// playEvolutionsLockR is where relationships are stored.
type playEvolutionsLockR struct {
}

// playEvolutionsLockL is where Load methods for each relationship are stored.
type playEvolutionsLockL struct{}

var (
	playEvolutionsLockColumns               = []string{"lock"}
	playEvolutionsLockColumnsWithoutDefault = []string{"lock"}
	playEvolutionsLockColumnsWithDefault    = []string{}
	playEvolutionsLockColumnsWithCustom     = []string{}

	playEvolutionsLockPrimaryKeyColumns = []string{"lock"}
)

type (
	// PlayEvolutionsLockSlice is an alias for a slice of pointers to PlayEvolutionsLock.
	// This should generally be used opposed to []PlayEvolutionsLock.
	PlayEvolutionsLockSlice []*PlayEvolutionsLock
	// PlayEvolutionsLockHook is the signature for custom PlayEvolutionsLock hook methods
	PlayEvolutionsLockHook func(boil.Executor, *PlayEvolutionsLock) error

	playEvolutionsLockQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	playEvolutionsLockType    = reflect.TypeOf(&PlayEvolutionsLock{})
	playEvolutionsLockMapping = queries.MakeStructMapping(playEvolutionsLockType)

	playEvolutionsLockPrimaryKeyMapping, _ = queries.BindMapping(playEvolutionsLockType, playEvolutionsLockMapping, playEvolutionsLockPrimaryKeyColumns)

	playEvolutionsLockInsertCacheMut sync.RWMutex
	playEvolutionsLockInsertCache    = make(map[string]insertCache)
	playEvolutionsLockUpdateCacheMut sync.RWMutex
	playEvolutionsLockUpdateCache    = make(map[string]updateCache)
	playEvolutionsLockUpsertCacheMut sync.RWMutex
	playEvolutionsLockUpsertCache    = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var playEvolutionsLockBeforeInsertHooks []PlayEvolutionsLockHook
var playEvolutionsLockBeforeUpdateHooks []PlayEvolutionsLockHook
var playEvolutionsLockBeforeDeleteHooks []PlayEvolutionsLockHook
var playEvolutionsLockBeforeUpsertHooks []PlayEvolutionsLockHook

var playEvolutionsLockAfterInsertHooks []PlayEvolutionsLockHook
var playEvolutionsLockAfterSelectHooks []PlayEvolutionsLockHook
var playEvolutionsLockAfterUpdateHooks []PlayEvolutionsLockHook
var playEvolutionsLockAfterDeleteHooks []PlayEvolutionsLockHook
var playEvolutionsLockAfterUpsertHooks []PlayEvolutionsLockHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PlayEvolutionsLock) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PlayEvolutionsLock) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PlayEvolutionsLock) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PlayEvolutionsLock) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PlayEvolutionsLock) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PlayEvolutionsLock) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PlayEvolutionsLock) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PlayEvolutionsLock) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PlayEvolutionsLock) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range playEvolutionsLockAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPlayEvolutionsLockHook registers your hook function for all future operations.
func AddPlayEvolutionsLockHook(hookPoint boil.HookPoint, playEvolutionsLockHook PlayEvolutionsLockHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		playEvolutionsLockBeforeInsertHooks = append(playEvolutionsLockBeforeInsertHooks, playEvolutionsLockHook)
	case boil.BeforeUpdateHook:
		playEvolutionsLockBeforeUpdateHooks = append(playEvolutionsLockBeforeUpdateHooks, playEvolutionsLockHook)
	case boil.BeforeDeleteHook:
		playEvolutionsLockBeforeDeleteHooks = append(playEvolutionsLockBeforeDeleteHooks, playEvolutionsLockHook)
	case boil.BeforeUpsertHook:
		playEvolutionsLockBeforeUpsertHooks = append(playEvolutionsLockBeforeUpsertHooks, playEvolutionsLockHook)
	case boil.AfterInsertHook:
		playEvolutionsLockAfterInsertHooks = append(playEvolutionsLockAfterInsertHooks, playEvolutionsLockHook)
	case boil.AfterSelectHook:
		playEvolutionsLockAfterSelectHooks = append(playEvolutionsLockAfterSelectHooks, playEvolutionsLockHook)
	case boil.AfterUpdateHook:
		playEvolutionsLockAfterUpdateHooks = append(playEvolutionsLockAfterUpdateHooks, playEvolutionsLockHook)
	case boil.AfterDeleteHook:
		playEvolutionsLockAfterDeleteHooks = append(playEvolutionsLockAfterDeleteHooks, playEvolutionsLockHook)
	case boil.AfterUpsertHook:
		playEvolutionsLockAfterUpsertHooks = append(playEvolutionsLockAfterUpsertHooks, playEvolutionsLockHook)
	}
}

// OneP returns a single playEvolutionsLock record from the query, and panics on error.
func (q playEvolutionsLockQuery) OneP() *PlayEvolutionsLock {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single playEvolutionsLock record from the query.
func (q playEvolutionsLockQuery) One() (*PlayEvolutionsLock, error) {
	o := &PlayEvolutionsLock{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "public: failed to execute a one query for play_evolutions_lock")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all PlayEvolutionsLock records from the query, and panics on error.
func (q playEvolutionsLockQuery) AllP() PlayEvolutionsLockSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all PlayEvolutionsLock records from the query.
func (q playEvolutionsLockQuery) All() (PlayEvolutionsLockSlice, error) {
	var o PlayEvolutionsLockSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "public: failed to assign all query results to PlayEvolutionsLock slice")
	}

	if len(playEvolutionsLockAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all PlayEvolutionsLock records in the query, and panics on error.
func (q playEvolutionsLockQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all PlayEvolutionsLock records in the query.
func (q playEvolutionsLockQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "public: failed to count play_evolutions_lock rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q playEvolutionsLockQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q playEvolutionsLockQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "public: failed to check if play_evolutions_lock exists")
	}

	return count > 0, nil
}

// PlayEvolutionsLocksG retrieves all records.
func PlayEvolutionsLocksG(mods ...qm.QueryMod) playEvolutionsLockQuery {
	return PlayEvolutionsLocks(boil.GetDB(), mods...)
}

// PlayEvolutionsLocks retrieves all the records using an executor.
func PlayEvolutionsLocks(exec boil.Executor, mods ...qm.QueryMod) playEvolutionsLockQuery {
	mods = append(mods, qm.From("\"play_evolutions_lock\""))
	return playEvolutionsLockQuery{NewQuery(exec, mods...)}
}

// FindPlayEvolutionsLockG retrieves a single record by ID.
func FindPlayEvolutionsLockG(lock int, selectCols ...string) (*PlayEvolutionsLock, error) {
	return FindPlayEvolutionsLock(boil.GetDB(), lock, selectCols...)
}

// FindPlayEvolutionsLockGP retrieves a single record by ID, and panics on error.
func FindPlayEvolutionsLockGP(lock int, selectCols ...string) *PlayEvolutionsLock {
	retobj, err := FindPlayEvolutionsLock(boil.GetDB(), lock, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindPlayEvolutionsLock retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPlayEvolutionsLock(exec boil.Executor, lock int, selectCols ...string) (*PlayEvolutionsLock, error) {
	playEvolutionsLockObj := &PlayEvolutionsLock{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"play_evolutions_lock\" where \"lock\"=$1", sel,
	)

	q := queries.Raw(exec, query, lock)

	err := q.Bind(playEvolutionsLockObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "public: unable to select from play_evolutions_lock")
	}

	return playEvolutionsLockObj, nil
}

// FindPlayEvolutionsLockP retrieves a single record by ID with an executor, and panics on error.
func FindPlayEvolutionsLockP(exec boil.Executor, lock int, selectCols ...string) *PlayEvolutionsLock {
	retobj, err := FindPlayEvolutionsLock(exec, lock, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *PlayEvolutionsLock) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *PlayEvolutionsLock) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *PlayEvolutionsLock) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *PlayEvolutionsLock) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("public: no play_evolutions_lock provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(playEvolutionsLockColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	playEvolutionsLockInsertCacheMut.RLock()
	cache, cached := playEvolutionsLockInsertCache[key]
	playEvolutionsLockInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			playEvolutionsLockColumns,
			playEvolutionsLockColumnsWithDefault,
			playEvolutionsLockColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(playEvolutionsLockType, playEvolutionsLockMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(playEvolutionsLockType, playEvolutionsLockMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"play_evolutions_lock\" (\"%s\") VALUES (%s)", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"play_evolutions_lock\" DEFAULT VALUES"
		}

		if len(cache.retMapping) != 0 {
			cache.query += fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "public: unable to insert into play_evolutions_lock")
	}

	if !cached {
		playEvolutionsLockInsertCacheMut.Lock()
		playEvolutionsLockInsertCache[key] = cache
		playEvolutionsLockInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single PlayEvolutionsLock record. See Update for
// whitelist behavior description.
func (o *PlayEvolutionsLock) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single PlayEvolutionsLock record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *PlayEvolutionsLock) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the PlayEvolutionsLock, and panics on error.
// See Update for whitelist behavior description.
func (o *PlayEvolutionsLock) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the PlayEvolutionsLock.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *PlayEvolutionsLock) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	playEvolutionsLockUpdateCacheMut.RLock()
	cache, cached := playEvolutionsLockUpdateCache[key]
	playEvolutionsLockUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(playEvolutionsLockColumns, playEvolutionsLockPrimaryKeyColumns, whitelist)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("public: unable to update play_evolutions_lock, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"play_evolutions_lock\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, playEvolutionsLockPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(playEvolutionsLockType, playEvolutionsLockMapping, append(wl, playEvolutionsLockPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "public: unable to update play_evolutions_lock row")
	}

	if !cached {
		playEvolutionsLockUpdateCacheMut.Lock()
		playEvolutionsLockUpdateCache[key] = cache
		playEvolutionsLockUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q playEvolutionsLockQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q playEvolutionsLockQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "public: unable to update all for play_evolutions_lock")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o PlayEvolutionsLockSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o PlayEvolutionsLockSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o PlayEvolutionsLockSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PlayEvolutionsLockSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("public: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playEvolutionsLockPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"UPDATE \"play_evolutions_lock\" SET %s WHERE (\"lock\") IN (%s)",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(playEvolutionsLockPrimaryKeyColumns), len(colNames)+1, len(playEvolutionsLockPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to update all in playEvolutionsLock slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *PlayEvolutionsLock) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *PlayEvolutionsLock) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *PlayEvolutionsLock) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *PlayEvolutionsLock) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("public: no play_evolutions_lock provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(playEvolutionsLockColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	playEvolutionsLockUpsertCacheMut.RLock()
	cache, cached := playEvolutionsLockUpsertCache[key]
	playEvolutionsLockUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			playEvolutionsLockColumns,
			playEvolutionsLockColumnsWithDefault,
			playEvolutionsLockColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			playEvolutionsLockColumns,
			playEvolutionsLockPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("public: unable to upsert play_evolutions_lock, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(playEvolutionsLockPrimaryKeyColumns))
			copy(conflict, playEvolutionsLockPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"play_evolutions_lock\"", updateOnConflict, ret, update, conflict, whitelist)

		cache.valueMapping, err = queries.BindMapping(playEvolutionsLockType, playEvolutionsLockMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(playEvolutionsLockType, playEvolutionsLockMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "public: unable to upsert play_evolutions_lock")
	}

	if !cached {
		playEvolutionsLockUpsertCacheMut.Lock()
		playEvolutionsLockUpsertCache[key] = cache
		playEvolutionsLockUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single PlayEvolutionsLock record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PlayEvolutionsLock) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single PlayEvolutionsLock record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *PlayEvolutionsLock) DeleteG() error {
	if o == nil {
		return errors.New("public: no PlayEvolutionsLock provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single PlayEvolutionsLock record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PlayEvolutionsLock) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single PlayEvolutionsLock record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PlayEvolutionsLock) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("public: no PlayEvolutionsLock provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), playEvolutionsLockPrimaryKeyMapping)
	query := "DELETE FROM \"play_evolutions_lock\" WHERE \"lock\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to delete from play_evolutions_lock")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q playEvolutionsLockQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q playEvolutionsLockQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("public: no playEvolutionsLockQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "public: unable to delete all from play_evolutions_lock")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o PlayEvolutionsLockSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o PlayEvolutionsLockSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("public: no PlayEvolutionsLock slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o PlayEvolutionsLockSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PlayEvolutionsLockSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("public: no PlayEvolutionsLock slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(playEvolutionsLockBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playEvolutionsLockPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"DELETE FROM \"play_evolutions_lock\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, playEvolutionsLockPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(playEvolutionsLockPrimaryKeyColumns), 1, len(playEvolutionsLockPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to delete all from playEvolutionsLock slice")
	}

	if len(playEvolutionsLockAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *PlayEvolutionsLock) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *PlayEvolutionsLock) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *PlayEvolutionsLock) ReloadG() error {
	if o == nil {
		return errors.New("public: no PlayEvolutionsLock provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PlayEvolutionsLock) Reload(exec boil.Executor) error {
	ret, err := FindPlayEvolutionsLock(exec, o.Lock)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PlayEvolutionsLockSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PlayEvolutionsLockSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlayEvolutionsLockSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("public: empty PlayEvolutionsLockSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlayEvolutionsLockSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	playEvolutionsLocks := PlayEvolutionsLockSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playEvolutionsLockPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"SELECT \"play_evolutions_lock\".* FROM \"play_evolutions_lock\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, playEvolutionsLockPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(playEvolutionsLockPrimaryKeyColumns), 1, len(playEvolutionsLockPrimaryKeyColumns)),
	)

	q := queries.Raw(exec, query, args...)

	err := q.Bind(&playEvolutionsLocks)
	if err != nil {
		return errors.Wrap(err, "public: unable to reload all in PlayEvolutionsLockSlice")
	}

	*o = playEvolutionsLocks

	return nil
}

// PlayEvolutionsLockExists checks if the PlayEvolutionsLock row exists.
func PlayEvolutionsLockExists(exec boil.Executor, lock int) (bool, error) {
	var exists bool

	query := "select exists(select 1 from \"play_evolutions_lock\" where \"lock\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, lock)
	}

	row := exec.QueryRow(query, lock)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "public: unable to check if play_evolutions_lock exists")
	}

	return exists, nil
}

// PlayEvolutionsLockExistsG checks if the PlayEvolutionsLock row exists.
func PlayEvolutionsLockExistsG(lock int) (bool, error) {
	return PlayEvolutionsLockExists(boil.GetDB(), lock)
}

// PlayEvolutionsLockExistsGP checks if the PlayEvolutionsLock row exists. Panics on error.
func PlayEvolutionsLockExistsGP(lock int) bool {
	e, err := PlayEvolutionsLockExists(boil.GetDB(), lock)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// PlayEvolutionsLockExistsP checks if the PlayEvolutionsLock row exists. Panics on error.
func PlayEvolutionsLockExistsP(exec boil.Executor, lock int) bool {
	e, err := PlayEvolutionsLockExists(exec, lock)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
