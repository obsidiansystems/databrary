// This file is generated by SQLBoiler (https://github.com/databrary/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// EDIT AT YOUR OWN RISK

package public

import (
	"bytes"
	"github.com/databrary/databrary/db/models/custom_types"
	"github.com/databrary/sqlboiler/boil"
	"github.com/databrary/sqlboiler/randomize"
	"github.com/databrary/sqlboiler/strmangle"
	"github.com/pmezard/go-difflib/difflib"
	"os"
	"os/exec"
	"reflect"
	"sort"
	"strings"
	"testing"
)

func testAvatars(t *testing.T) {
	t.Parallel()

	query := Avatars(nil)

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testAvatarsLive(t *testing.T) {
	all, err := Avatars(dbMain.liveDbConn).All()
	if err != nil {
		t.Fatalf("failed to get all Avatars err: ", err)
	}
	tx, err := dbMain.liveTestDbConn.Begin()
	if err != nil {
		t.Fatalf("failed to begin transaction: ", err)
	}
	for _, v := range all {
		err := v.Insert(tx)
		if err != nil {
			t.Fatalf("failed to failed to insert %s because of %s", v, err)
		}

	}
	err = tx.Commit()
	if err != nil {
		t.Fatalf("failed to commit transaction: ", err)
	}
	bf := &bytes.Buffer{}
	dumpCmd := exec.Command("psql", `-c "COPY (SELECT * FROM avatar) TO STDOUT" -d `, dbMain.DbName)
	dumpCmd.Env = append(os.Environ(), dbMain.pgEnv()...)
	dumpCmd.Stdout = bf
	err = dumpCmd.Start()
	if err != nil {
		t.Fatalf("failed to start dump from live db because of %s", err)
	}
	dumpCmd.Wait()
	if err != nil {
		t.Fatalf("failed to wait dump from live db because of %s", err)
	}
	bg := &bytes.Buffer{}
	dumpCmd = exec.Command("psql", `-c "COPY (SELECT * FROM avatar) TO STDOUT" -d `, dbMain.LiveTestDBName)
	dumpCmd.Env = append(os.Environ(), dbMain.pgEnv()...)
	dumpCmd.Stdout = bg
	err = dumpCmd.Start()
	if err != nil {
		t.Fatalf("failed to start dump from test db because of %s", err)
	}
	dumpCmd.Wait()
	if err != nil {
		t.Fatalf("failed to wait dump from test db because of %s", err)
	}
	bfslice := sort.StringSlice(difflib.SplitLines(bf.String()))
	gfslice := sort.StringSlice(difflib.SplitLines(bg.String()))
	bfslice.Sort()
	gfslice.Sort()
	diff := difflib.ContextDiff{
		A:        bfslice,
		B:        gfslice,
		FromFile: "databrary",
		ToFile:   "test",
		Context:  1,
	}
	result, _ := difflib.GetContextDiffString(diff)
	if len(result) > 0 {
		t.Fatalf("AvatarsLive failed but it's probably trivial: %s", strings.Replace(result, "\t", " ", -1))
	}

}

func testAvatarsDelete(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = avatar.Delete(tx); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAvatarsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = Avatars(tx).DeleteAll(); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAvatarsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := AvatarSlice{avatar}

	if err = slice.DeleteAll(tx); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAvatarsExists(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	e, err := AvatarExists(tx, avatar.Party)
	if err != nil {
		t.Errorf("Unable to check if Avatar exists: %s", err)
	}
	if !e {
		t.Errorf("Expected AvatarExistsG to return true, but got false.")
	}
}

func testAvatarsFind(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	avatarFound, err := FindAvatar(tx, avatar.Party)
	if err != nil {
		t.Error(err)
	}

	if avatarFound == nil {
		t.Error("want a record, got nil")
	}
}

func testAvatarsBind(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = Avatars(tx).Bind(avatar); err != nil {
		t.Error(err)
	}
}

func testAvatarsOne(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	if x, err := Avatars(tx).One(); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testAvatarsAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatarOne := &Avatar{}
	avatarTwo := &Avatar{}
	if err = randomize.Struct(seed, avatarOne, avatarDBTypes, false, avatarColumnsWithDefault...); err != nil {

		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}
	if err = randomize.Struct(seed, avatarTwo, avatarDBTypes, false, avatarColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatarOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = avatarTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := Avatars(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testAvatarsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatarOne := &Avatar{}
	avatarTwo := &Avatar{}
	if err = randomize.Struct(seed, avatarOne, avatarDBTypes, false, avatarColumnsWithDefault...); err != nil {

		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}
	if err = randomize.Struct(seed, avatarTwo, avatarDBTypes, false, avatarColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatarOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = avatarTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func avatarBeforeInsertHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarAfterInsertHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarAfterSelectHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarBeforeUpdateHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarAfterUpdateHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarBeforeDeleteHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarAfterDeleteHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarBeforeUpsertHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func avatarAfterUpsertHook(e boil.Executor, o *Avatar) error {
	*o = Avatar{}
	return nil
}

func testAvatarsHooks(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	empty := &Avatar{}

	AddAvatarHook(boil.BeforeInsertHook, avatarBeforeInsertHook)
	if err = avatar.doBeforeInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", avatar)
	}
	avatarBeforeInsertHooks = []AvatarHook{}

	AddAvatarHook(boil.AfterInsertHook, avatarAfterInsertHook)
	if err = avatar.doAfterInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", avatar)
	}
	avatarAfterInsertHooks = []AvatarHook{}

	AddAvatarHook(boil.AfterSelectHook, avatarAfterSelectHook)
	if err = avatar.doAfterSelectHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", avatar)
	}
	avatarAfterSelectHooks = []AvatarHook{}

	AddAvatarHook(boil.BeforeUpdateHook, avatarBeforeUpdateHook)
	if err = avatar.doBeforeUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", avatar)
	}
	avatarBeforeUpdateHooks = []AvatarHook{}

	AddAvatarHook(boil.AfterUpdateHook, avatarAfterUpdateHook)
	if err = avatar.doAfterUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", avatar)
	}
	avatarAfterUpdateHooks = []AvatarHook{}

	AddAvatarHook(boil.BeforeDeleteHook, avatarBeforeDeleteHook)
	if err = avatar.doBeforeDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", avatar)
	}
	avatarBeforeDeleteHooks = []AvatarHook{}

	AddAvatarHook(boil.AfterDeleteHook, avatarAfterDeleteHook)
	if err = avatar.doAfterDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", avatar)
	}
	avatarAfterDeleteHooks = []AvatarHook{}

	AddAvatarHook(boil.BeforeUpsertHook, avatarBeforeUpsertHook)
	if err = avatar.doBeforeUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", avatar)
	}
	avatarBeforeUpsertHooks = []AvatarHook{}

	AddAvatarHook(boil.AfterUpsertHook, avatarAfterUpsertHook)
	if err = avatar.doAfterUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(avatar, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", avatar)
	}
	avatarAfterUpsertHooks = []AvatarHook{}
}
func testAvatarsInsert(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAvatarsInsertWhitelist(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx, avatarColumns...); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAvatarToOneAssetUsingAsset(t *testing.T) {
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	seed := randomize.NewSeed()

	var foreign Asset
	var local Avatar

	foreignBlacklist := assetColumnsWithDefault
	foreignBlacklist = append(foreignBlacklist, assetColumnsWithCustom...)

	if err := randomize.Struct(seed, &foreign, assetDBTypes, true, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Asset struct: %s", err)
	}
	foreign.Release = custom_types.NullReleaseRandom()
	foreign.Duration = custom_types.NullIntervalRandom()

	localBlacklist := avatarColumnsWithDefault
	if err := randomize.Struct(seed, &local, avatarDBTypes, true, localBlacklist...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	if err := foreign.Insert(tx); err != nil {
		t.Fatal(err)
	}

	local.Asset = foreign.ID
	if err := local.Insert(tx); err != nil {
		t.Fatal(err)
	}

	check, err := local.AssetByFk(tx).One()
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := AvatarSlice{&local}
	if err = local.L.LoadAsset(tx, false, &slice); err != nil {
		t.Fatal(err)
	}
	if local.R.Asset == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Asset = nil
	if err = local.L.LoadAsset(tx, true, &local); err != nil {
		t.Fatal(err)
	}
	if local.R.Asset == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testAvatarToOnePartyUsingParty(t *testing.T) {
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	seed := randomize.NewSeed()

	var foreign Party
	var local Avatar

	foreignBlacklist := partyColumnsWithDefault
	if err := randomize.Struct(seed, &foreign, partyDBTypes, true, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Party struct: %s", err)
	}
	localBlacklist := avatarColumnsWithDefault
	if err := randomize.Struct(seed, &local, avatarDBTypes, true, localBlacklist...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	if err := foreign.Insert(tx); err != nil {
		t.Fatal(err)
	}

	local.Party = foreign.ID
	if err := local.Insert(tx); err != nil {
		t.Fatal(err)
	}

	check, err := local.PartyByFk(tx).One()
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := AvatarSlice{&local}
	if err = local.L.LoadParty(tx, false, &slice); err != nil {
		t.Fatal(err)
	}
	if local.R.Party == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Party = nil
	if err = local.L.LoadParty(tx, true, &local); err != nil {
		t.Fatal(err)
	}
	if local.R.Party == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testAvatarToOneSetOpAssetUsingAsset(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	seed := randomize.NewSeed()

	var a Avatar
	var b, c Asset

	foreignBlacklist := strmangle.SetComplement(assetPrimaryKeyColumns, assetColumnsWithoutDefault)
	foreignBlacklist = append(foreignBlacklist, assetColumnsWithCustom...)

	if err := randomize.Struct(seed, &b, assetDBTypes, false, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Asset struct: %s", err)
	}
	if err := randomize.Struct(seed, &c, assetDBTypes, false, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Asset struct: %s", err)
	}
	b.Release = custom_types.NullReleaseRandom()
	c.Release = custom_types.NullReleaseRandom()
	b.Duration = custom_types.NullIntervalRandom()
	c.Duration = custom_types.NullIntervalRandom()

	localBlacklist := strmangle.SetComplement(avatarPrimaryKeyColumns, avatarColumnsWithoutDefault)
	if err := randomize.Struct(seed, &a, avatarDBTypes, false, localBlacklist...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Asset{&b, &c} {
		err = a.SetAsset(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Asset != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Avatars[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.Asset != x.ID {
			t.Error("foreign key was wrong value", a.Asset)
		}

		zero := reflect.Zero(reflect.TypeOf(a.Asset))
		reflect.Indirect(reflect.ValueOf(&a.Asset)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.Asset != x.ID {
			t.Error("foreign key was wrong value", a.Asset, x.ID)
		}
	}
}
func testAvatarToOneSetOpPartyUsingParty(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	seed := randomize.NewSeed()

	var a Avatar
	var b, c Party

	foreignBlacklist := strmangle.SetComplement(partyPrimaryKeyColumns, partyColumnsWithoutDefault)
	if err := randomize.Struct(seed, &b, partyDBTypes, false, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Party struct: %s", err)
	}
	if err := randomize.Struct(seed, &c, partyDBTypes, false, foreignBlacklist...); err != nil {
		t.Errorf("Unable to randomize Party struct: %s", err)
	}
	localBlacklist := strmangle.SetComplement(avatarPrimaryKeyColumns, avatarColumnsWithoutDefault)
	if err := randomize.Struct(seed, &a, avatarDBTypes, false, localBlacklist...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Party{&b, &c} {
		err = a.SetParty(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Party != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Avatar != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.Party != x.ID {
			t.Error("foreign key was wrong value", a.Party)
		}

		if exists, err := AvatarExists(tx, a.Party); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}

func testAvatarsReload(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = avatar.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testAvatarsReloadAll(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := AvatarSlice{avatar}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testAvatarsSelect(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := Avatars(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	avatarDBTypes = map[string]string{`Asset`: `integer`, `Party`: `integer`}
	_             = bytes.MinRead
)

func testAvatarsUpdate(t *testing.T) {
	t.Parallel()

	if len(avatarColumns) == len(avatarPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	blacklist := avatarColumnsWithDefault

	if err = randomize.Struct(seed, avatar, avatarDBTypes, true, blacklist...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	if err = avatar.Update(tx); err != nil {
		t.Error(err)
	}
}

func testAvatarsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(avatarColumns) == len(avatarPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	blacklist := avatarPrimaryKeyColumns

	if err = randomize.Struct(seed, avatar, avatarDBTypes, true, blacklist...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(avatarColumns, avatarPrimaryKeyColumns) {
		fields = avatarColumns
	} else {
		fields = strmangle.SetComplement(
			avatarColumns,
			avatarPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(avatar))
	updateMap := M{}
	for _, col := range fields {
		updateMap[col] = value.FieldByName(strmangle.TitleCase(col)).Interface()
	}

	slice := AvatarSlice{avatar}
	if err = slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	}
}

func testAvatarsUpsert(t *testing.T) {
	t.Parallel()

	if len(avatarColumns) == len(avatarPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	var err error
	seed := randomize.NewSeed()
	avatar := &Avatar{}
	if err = randomize.Struct(seed, avatar, avatarDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = avatar.Upsert(tx, false, nil, nil); err != nil {
		t.Errorf("Unable to upsert Avatar: %s", err)
	}

	count, err := Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	blacklist := avatarPrimaryKeyColumns

	if err = randomize.Struct(seed, avatar, avatarDBTypes, false, blacklist...); err != nil {
		t.Errorf("Unable to randomize Avatar struct: %s", err)
	}

	if err = avatar.Upsert(tx, true, nil, nil); err != nil {
		t.Errorf("Unable to upsert Avatar: %s", err)
	}

	count, err = Avatars(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
