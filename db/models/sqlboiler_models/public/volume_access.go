// This file is generated by SQLBoiler (https://github.com/databrary/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// EDIT AT YOUR OWN RISK

package public

import (
	"bytes"
	"database/sql"
	"fmt"
	"github.com/databrary/databrary/db/models/custom_types"
	"github.com/databrary/sqlboiler/boil"
	"github.com/databrary/sqlboiler/queries"
	"github.com/databrary/sqlboiler/queries/qm"
	"github.com/databrary/sqlboiler/strmangle"
	"github.com/pkg/errors"
	"gopkg.in/nullbio/null.v6"
	"reflect"
	"strings"
	"sync"
	"time"
)

// VolumeAccess is an object representing the database table.
type VolumeAccess struct {
	Volume     int                     `db:"volume" json:"volumeAccess_volume"`
	Party      int                     `db:"party" json:"volumeAccess_party"`
	Individual custom_types.Permission `db:"individual" json:"volumeAccess_individual"`
	Children   custom_types.Permission `db:"children" json:"volumeAccess_children"`
	Sort       null.Int16              `db:"sort" json:"volumeAccess_sort,omitempty"`

	R *volumeAccessR `db:"-" json:"-"`
	L volumeAccessL  `db:"-" json:"-"`
}

// volumeAccessR is where relationships are stored.
type volumeAccessR struct {
	Party  *Party
	Volume *Volume
}

// volumeAccessL is where Load methods for each relationship are stored.
type volumeAccessL struct{}

var (
	volumeAccessColumns               = []string{"volume", "party", "individual", "children", "sort"}
	volumeAccessColumnsWithoutDefault = []string{"volume", "party", "sort"}
	volumeAccessColumnsWithDefault    = []string{"individual", "children"}
	volumeAccessColumnsWithCustom     = []string{"individual", "children"}

	volumeAccessPrimaryKeyColumns = []string{"volume", "party"}
)

type (
	// VolumeAccessSlice is an alias for a slice of pointers to VolumeAccess.
	// This should generally be used opposed to []VolumeAccess.
	VolumeAccessSlice []*VolumeAccess
	// VolumeAccessHook is the signature for custom VolumeAccess hook methods
	VolumeAccessHook func(boil.Executor, *VolumeAccess) error

	volumeAccessQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	volumeAccessType    = reflect.TypeOf(&VolumeAccess{})
	volumeAccessMapping = queries.MakeStructMapping(volumeAccessType)

	volumeAccessPrimaryKeyMapping, _ = queries.BindMapping(volumeAccessType, volumeAccessMapping, volumeAccessPrimaryKeyColumns)

	volumeAccessInsertCacheMut sync.RWMutex
	volumeAccessInsertCache    = make(map[string]insertCache)
	volumeAccessUpdateCacheMut sync.RWMutex
	volumeAccessUpdateCache    = make(map[string]updateCache)
	volumeAccessUpsertCacheMut sync.RWMutex
	volumeAccessUpsertCache    = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var volumeAccessBeforeInsertHooks []VolumeAccessHook
var volumeAccessBeforeUpdateHooks []VolumeAccessHook
var volumeAccessBeforeDeleteHooks []VolumeAccessHook
var volumeAccessBeforeUpsertHooks []VolumeAccessHook

var volumeAccessAfterInsertHooks []VolumeAccessHook
var volumeAccessAfterSelectHooks []VolumeAccessHook
var volumeAccessAfterUpdateHooks []VolumeAccessHook
var volumeAccessAfterDeleteHooks []VolumeAccessHook
var volumeAccessAfterUpsertHooks []VolumeAccessHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *VolumeAccess) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *VolumeAccess) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *VolumeAccess) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *VolumeAccess) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *VolumeAccess) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *VolumeAccess) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *VolumeAccess) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *VolumeAccess) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *VolumeAccess) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeAccessAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVolumeAccessHook registers your hook function for all future operations.
func AddVolumeAccessHook(hookPoint boil.HookPoint, volumeAccessHook VolumeAccessHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		volumeAccessBeforeInsertHooks = append(volumeAccessBeforeInsertHooks, volumeAccessHook)
	case boil.BeforeUpdateHook:
		volumeAccessBeforeUpdateHooks = append(volumeAccessBeforeUpdateHooks, volumeAccessHook)
	case boil.BeforeDeleteHook:
		volumeAccessBeforeDeleteHooks = append(volumeAccessBeforeDeleteHooks, volumeAccessHook)
	case boil.BeforeUpsertHook:
		volumeAccessBeforeUpsertHooks = append(volumeAccessBeforeUpsertHooks, volumeAccessHook)
	case boil.AfterInsertHook:
		volumeAccessAfterInsertHooks = append(volumeAccessAfterInsertHooks, volumeAccessHook)
	case boil.AfterSelectHook:
		volumeAccessAfterSelectHooks = append(volumeAccessAfterSelectHooks, volumeAccessHook)
	case boil.AfterUpdateHook:
		volumeAccessAfterUpdateHooks = append(volumeAccessAfterUpdateHooks, volumeAccessHook)
	case boil.AfterDeleteHook:
		volumeAccessAfterDeleteHooks = append(volumeAccessAfterDeleteHooks, volumeAccessHook)
	case boil.AfterUpsertHook:
		volumeAccessAfterUpsertHooks = append(volumeAccessAfterUpsertHooks, volumeAccessHook)
	}
}

// OneP returns a single volumeAccess record from the query, and panics on error.
func (q volumeAccessQuery) OneP() *VolumeAccess {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single volumeAccess record from the query.
func (q volumeAccessQuery) One() (*VolumeAccess, error) {
	o := &VolumeAccess{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "public: failed to execute a one query for volume_access")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all VolumeAccess records from the query, and panics on error.
func (q volumeAccessQuery) AllP() VolumeAccessSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all VolumeAccess records from the query.
func (q volumeAccessQuery) All() (VolumeAccessSlice, error) {
	var o VolumeAccessSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "public: failed to assign all query results to VolumeAccess slice")
	}

	if len(volumeAccessAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all VolumeAccess records in the query, and panics on error.
func (q volumeAccessQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all VolumeAccess records in the query.
func (q volumeAccessQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "public: failed to count volume_access rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q volumeAccessQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q volumeAccessQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "public: failed to check if volume_access exists")
	}

	return count > 0, nil
}

// PartyG pointed to by the foreign key.
func (o *VolumeAccess) PartyG(mods ...qm.QueryMod) partyQuery {
	return o.PartyByFk(boil.GetDB(), mods...)
}

// Party pointed to by the foreign key.
func (o *VolumeAccess) PartyByFk(exec boil.Executor, mods ...qm.QueryMod) partyQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.Party),
	}

	queryMods = append(queryMods, mods...)

	query := Parties(exec, queryMods...)
	queries.SetFrom(query.Query, "\"party\"")

	return query
}

// VolumeG pointed to by the foreign key.
func (o *VolumeAccess) VolumeG(mods ...qm.QueryMod) volumeQuery {
	return o.VolumeByFk(boil.GetDB(), mods...)
}

// Volume pointed to by the foreign key.
func (o *VolumeAccess) VolumeByFk(exec boil.Executor, mods ...qm.QueryMod) volumeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.Volume),
	}

	queryMods = append(queryMods, mods...)

	query := Volumes(exec, queryMods...)
	queries.SetFrom(query.Query, "\"volume\"")

	return query
}

// LoadParty allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (volumeAccessL) LoadParty(e boil.Executor, singular bool, maybeVolumeAccess interface{}) error {
	var slice []*VolumeAccess
	var object *VolumeAccess

	count := 1
	if singular {
		object = maybeVolumeAccess.(*VolumeAccess)
	} else {
		slice = *maybeVolumeAccess.(*VolumeAccessSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &volumeAccessR{}
		}
		args[0] = object.Party
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &volumeAccessR{}
			}
			args[i] = obj.Party
		}
	}

	query := fmt.Sprintf(
		"select * from \"party\" where \"id\" in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Party")
	}
	defer results.Close()

	var resultSlice []*Party
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Party")
	}

	if len(volumeAccessAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Party = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Party == foreign.ID {
				local.R.Party = foreign
				break
			}
		}
	}

	return nil
}

// LoadVolume allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (volumeAccessL) LoadVolume(e boil.Executor, singular bool, maybeVolumeAccess interface{}) error {
	var slice []*VolumeAccess
	var object *VolumeAccess

	count := 1
	if singular {
		object = maybeVolumeAccess.(*VolumeAccess)
	} else {
		slice = *maybeVolumeAccess.(*VolumeAccessSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &volumeAccessR{}
		}
		args[0] = object.Volume
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &volumeAccessR{}
			}
			args[i] = obj.Volume
		}
	}

	query := fmt.Sprintf(
		"select * from \"volume\" where \"id\" in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Volume")
	}
	defer results.Close()

	var resultSlice []*Volume
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Volume")
	}

	if len(volumeAccessAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Volume = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Volume == foreign.ID {
				local.R.Volume = foreign
				break
			}
		}
	}

	return nil
}

// SetPartyG of the volume_access to the related item.
// Sets o.R.Party to related.
// Adds o to related.R.VolumeAccesses.
// Uses the global database handle.
func (o *VolumeAccess) SetPartyG(insert bool, related *Party) error {
	return o.SetParty(boil.GetDB(), insert, related)
}

// SetPartyP of the volume_access to the related item.
// Sets o.R.Party to related.
// Adds o to related.R.VolumeAccesses.
// Panics on error.
func (o *VolumeAccess) SetPartyP(exec boil.Executor, insert bool, related *Party) {
	if err := o.SetParty(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetPartyGP of the volume_access to the related item.
// Sets o.R.Party to related.
// Adds o to related.R.VolumeAccesses.
// Uses the global database handle and panics on error.
func (o *VolumeAccess) SetPartyGP(insert bool, related *Party) {
	if err := o.SetParty(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetParty of the volume_access to the related item.
// Sets o.R.Party to related.
// Adds o to related.R.VolumeAccesses.
func (o *VolumeAccess) SetParty(exec boil.Executor, insert bool, related *Party) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"volume_access\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"party"}),
		strmangle.WhereClause("\"", "\"", 2, volumeAccessPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.Volume, o.Party}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Party = related.ID

	if o.R == nil {
		o.R = &volumeAccessR{
			Party: related,
		}
	} else {
		o.R.Party = related
	}

	if related.R == nil {
		related.R = &partyR{
			VolumeAccesses: VolumeAccessSlice{o},
		}
	} else {
		related.R.VolumeAccesses = append(related.R.VolumeAccesses, o)
	}

	return nil
}

// SetVolumeG of the volume_access to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeAccesses.
// Uses the global database handle.
func (o *VolumeAccess) SetVolumeG(insert bool, related *Volume) error {
	return o.SetVolume(boil.GetDB(), insert, related)
}

// SetVolumeP of the volume_access to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeAccesses.
// Panics on error.
func (o *VolumeAccess) SetVolumeP(exec boil.Executor, insert bool, related *Volume) {
	if err := o.SetVolume(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVolumeGP of the volume_access to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeAccesses.
// Uses the global database handle and panics on error.
func (o *VolumeAccess) SetVolumeGP(insert bool, related *Volume) {
	if err := o.SetVolume(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVolume of the volume_access to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeAccesses.
func (o *VolumeAccess) SetVolume(exec boil.Executor, insert bool, related *Volume) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"volume_access\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"volume"}),
		strmangle.WhereClause("\"", "\"", 2, volumeAccessPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.Volume, o.Party}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Volume = related.ID

	if o.R == nil {
		o.R = &volumeAccessR{
			Volume: related,
		}
	} else {
		o.R.Volume = related
	}

	if related.R == nil {
		related.R = &volumeR{
			VolumeAccesses: VolumeAccessSlice{o},
		}
	} else {
		related.R.VolumeAccesses = append(related.R.VolumeAccesses, o)
	}

	return nil
}

// VolumeAccessesG retrieves all records.
func VolumeAccessesG(mods ...qm.QueryMod) volumeAccessQuery {
	return VolumeAccesses(boil.GetDB(), mods...)
}

// VolumeAccesses retrieves all the records using an executor.
func VolumeAccesses(exec boil.Executor, mods ...qm.QueryMod) volumeAccessQuery {
	mods = append(mods, qm.From("\"volume_access\""))
	return volumeAccessQuery{NewQuery(exec, mods...)}
}

// FindVolumeAccessG retrieves a single record by ID.
func FindVolumeAccessG(volume int, party int, selectCols ...string) (*VolumeAccess, error) {
	return FindVolumeAccess(boil.GetDB(), volume, party, selectCols...)
}

// FindVolumeAccessGP retrieves a single record by ID, and panics on error.
func FindVolumeAccessGP(volume int, party int, selectCols ...string) *VolumeAccess {
	retobj, err := FindVolumeAccess(boil.GetDB(), volume, party, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindVolumeAccess retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVolumeAccess(exec boil.Executor, volume int, party int, selectCols ...string) (*VolumeAccess, error) {
	volumeAccessObj := &VolumeAccess{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"volume_access\" where \"volume\"=$1 AND \"party\"=$2", sel,
	)

	q := queries.Raw(exec, query, volume, party)

	err := q.Bind(volumeAccessObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "public: unable to select from volume_access")
	}

	return volumeAccessObj, nil
}

// FindVolumeAccessP retrieves a single record by ID with an executor, and panics on error.
func FindVolumeAccessP(exec boil.Executor, volume int, party int, selectCols ...string) *VolumeAccess {
	retobj, err := FindVolumeAccess(exec, volume, party, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *VolumeAccess) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *VolumeAccess) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *VolumeAccess) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *VolumeAccess) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("public: no volume_access provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(volumeAccessColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	volumeAccessInsertCacheMut.RLock()
	cache, cached := volumeAccessInsertCache[key]
	volumeAccessInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			volumeAccessColumns,
			volumeAccessColumnsWithDefault,
			volumeAccessColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(volumeAccessType, volumeAccessMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(volumeAccessType, volumeAccessMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"volume_access\" (\"%s\") VALUES (%s)", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"volume_access\" DEFAULT VALUES"
		}

		if len(cache.retMapping) != 0 {
			cache.query += fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "public: unable to insert into volume_access")
	}

	if !cached {
		volumeAccessInsertCacheMut.Lock()
		volumeAccessInsertCache[key] = cache
		volumeAccessInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single VolumeAccess record. See Update for
// whitelist behavior description.
func (o *VolumeAccess) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single VolumeAccess record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *VolumeAccess) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the VolumeAccess, and panics on error.
// See Update for whitelist behavior description.
func (o *VolumeAccess) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the VolumeAccess.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *VolumeAccess) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	volumeAccessUpdateCacheMut.RLock()
	cache, cached := volumeAccessUpdateCache[key]
	volumeAccessUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(volumeAccessColumns, volumeAccessPrimaryKeyColumns, whitelist)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("public: unable to update volume_access, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"volume_access\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, volumeAccessPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(volumeAccessType, volumeAccessMapping, append(wl, volumeAccessPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "public: unable to update volume_access row")
	}

	if !cached {
		volumeAccessUpdateCacheMut.Lock()
		volumeAccessUpdateCache[key] = cache
		volumeAccessUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q volumeAccessQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q volumeAccessQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "public: unable to update all for volume_access")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o VolumeAccessSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o VolumeAccessSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o VolumeAccessSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VolumeAccessSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("public: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), volumeAccessPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"UPDATE \"volume_access\" SET %s WHERE (\"volume\",\"party\") IN (%s)",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(volumeAccessPrimaryKeyColumns), len(colNames)+1, len(volumeAccessPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to update all in volumeAccess slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *VolumeAccess) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *VolumeAccess) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *VolumeAccess) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *VolumeAccess) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("public: no volume_access provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(volumeAccessColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	volumeAccessUpsertCacheMut.RLock()
	cache, cached := volumeAccessUpsertCache[key]
	volumeAccessUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			volumeAccessColumns,
			volumeAccessColumnsWithDefault,
			volumeAccessColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			volumeAccessColumns,
			volumeAccessPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("public: unable to upsert volume_access, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(volumeAccessPrimaryKeyColumns))
			copy(conflict, volumeAccessPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"volume_access\"", updateOnConflict, ret, update, conflict, whitelist)

		cache.valueMapping, err = queries.BindMapping(volumeAccessType, volumeAccessMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(volumeAccessType, volumeAccessMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "public: unable to upsert volume_access")
	}

	if !cached {
		volumeAccessUpsertCacheMut.Lock()
		volumeAccessUpsertCache[key] = cache
		volumeAccessUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single VolumeAccess record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *VolumeAccess) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single VolumeAccess record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *VolumeAccess) DeleteG() error {
	if o == nil {
		return errors.New("public: no VolumeAccess provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single VolumeAccess record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *VolumeAccess) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single VolumeAccess record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VolumeAccess) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("public: no VolumeAccess provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), volumeAccessPrimaryKeyMapping)
	query := "DELETE FROM \"volume_access\" WHERE \"volume\"=$1 AND \"party\"=$2"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to delete from volume_access")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q volumeAccessQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q volumeAccessQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("public: no volumeAccessQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "public: unable to delete all from volume_access")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o VolumeAccessSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o VolumeAccessSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("public: no VolumeAccess slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o VolumeAccessSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VolumeAccessSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("public: no VolumeAccess slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(volumeAccessBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), volumeAccessPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"DELETE FROM \"volume_access\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, volumeAccessPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(volumeAccessPrimaryKeyColumns), 1, len(volumeAccessPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(query, args...)
	if err != nil {
		return errors.Wrap(err, "public: unable to delete all from volumeAccess slice")
	}

	if len(volumeAccessAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *VolumeAccess) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *VolumeAccess) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *VolumeAccess) ReloadG() error {
	if o == nil {
		return errors.New("public: no VolumeAccess provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VolumeAccess) Reload(exec boil.Executor) error {
	ret, err := FindVolumeAccess(exec, o.Volume, o.Party)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *VolumeAccessSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *VolumeAccessSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VolumeAccessSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("public: empty VolumeAccessSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VolumeAccessSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	volumeAccesses := VolumeAccessSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), volumeAccessPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	query := fmt.Sprintf(
		"SELECT \"volume_access\".* FROM \"volume_access\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, volumeAccessPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(volumeAccessPrimaryKeyColumns), 1, len(volumeAccessPrimaryKeyColumns)),
	)

	q := queries.Raw(exec, query, args...)

	err := q.Bind(&volumeAccesses)
	if err != nil {
		return errors.Wrap(err, "public: unable to reload all in VolumeAccessSlice")
	}

	*o = volumeAccesses

	return nil
}

// VolumeAccessExists checks if the VolumeAccess row exists.
func VolumeAccessExists(exec boil.Executor, volume int, party int) (bool, error) {
	var exists bool

	query := "select exists(select 1 from \"volume_access\" where \"volume\"=$1 AND \"party\"=$2 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, volume, party)
	}

	row := exec.QueryRow(query, volume, party)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "public: unable to check if volume_access exists")
	}

	return exists, nil
}

// VolumeAccessExistsG checks if the VolumeAccess row exists.
func VolumeAccessExistsG(volume int, party int) (bool, error) {
	return VolumeAccessExists(boil.GetDB(), volume, party)
}

// VolumeAccessExistsGP checks if the VolumeAccess row exists. Panics on error.
func VolumeAccessExistsGP(volume int, party int) bool {
	e, err := VolumeAccessExists(boil.GetDB(), volume, party)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// VolumeAccessExistsP checks if the VolumeAccess row exists. Panics on error.
func VolumeAccessExistsP(exec boil.Executor, volume int, party int) bool {
	e, err := VolumeAccessExists(exec, volume, party)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
