// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/queries"
	"github.com/vattle/sqlboiler/queries/qm"
	"github.com/vattle/sqlboiler/strmangle"
)

// VolumeTextIdx is an object representing the database table.
type VolumeTextIdx struct {
	Volume int    `boil:"volume" json:"volume" toml:"volume" yaml:"volume"`
	TS     string `boil:"ts" json:"ts" toml:"ts" yaml:"ts"`

	R *volumeTextIdxR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L volumeTextIdxL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

// volumeTextIdxR is where relationships are stored.
type volumeTextIdxR struct {
	Volume *Volume
}

// volumeTextIdxL is where Load methods for each relationship are stored.
type volumeTextIdxL struct{}

var (
	volumeTextIdxColumns               = []string{"volume", "ts"}
	volumeTextIdxColumnsWithoutDefault = []string{"volume", "ts"}
	volumeTextIdxColumnsWithDefault    = []string{}
	volumeTextIdxPrimaryKeyColumns     = []string{"volume"}
)

type (
	// VolumeTextIdxSlice is an alias for a slice of pointers to VolumeTextIdx.
	// This should generally be used opposed to []VolumeTextIdx.
	VolumeTextIdxSlice []*VolumeTextIdx
	// VolumeTextIdxHook is the signature for custom VolumeTextIdx hook methods
	VolumeTextIdxHook func(boil.Executor, *VolumeTextIdx) error

	volumeTextIdxQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	volumeTextIdxType                 = reflect.TypeOf(&VolumeTextIdx{})
	volumeTextIdxMapping              = queries.MakeStructMapping(volumeTextIdxType)
	volumeTextIdxPrimaryKeyMapping, _ = queries.BindMapping(volumeTextIdxType, volumeTextIdxMapping, volumeTextIdxPrimaryKeyColumns)
	volumeTextIdxInsertCacheMut       sync.RWMutex
	volumeTextIdxInsertCache          = make(map[string]insertCache)
	volumeTextIdxUpdateCacheMut       sync.RWMutex
	volumeTextIdxUpdateCache          = make(map[string]updateCache)
	volumeTextIdxUpsertCacheMut       sync.RWMutex
	volumeTextIdxUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var volumeTextIdxBeforeInsertHooks []VolumeTextIdxHook
var volumeTextIdxBeforeUpdateHooks []VolumeTextIdxHook
var volumeTextIdxBeforeDeleteHooks []VolumeTextIdxHook
var volumeTextIdxBeforeUpsertHooks []VolumeTextIdxHook

var volumeTextIdxAfterInsertHooks []VolumeTextIdxHook
var volumeTextIdxAfterSelectHooks []VolumeTextIdxHook
var volumeTextIdxAfterUpdateHooks []VolumeTextIdxHook
var volumeTextIdxAfterDeleteHooks []VolumeTextIdxHook
var volumeTextIdxAfterUpsertHooks []VolumeTextIdxHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *VolumeTextIdx) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *VolumeTextIdx) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *VolumeTextIdx) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *VolumeTextIdx) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *VolumeTextIdx) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *VolumeTextIdx) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *VolumeTextIdx) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *VolumeTextIdx) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *VolumeTextIdx) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range volumeTextIdxAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVolumeTextIdxHook registers your hook function for all future operations.
func AddVolumeTextIdxHook(hookPoint boil.HookPoint, volumeTextIdxHook VolumeTextIdxHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		volumeTextIdxBeforeInsertHooks = append(volumeTextIdxBeforeInsertHooks, volumeTextIdxHook)
	case boil.BeforeUpdateHook:
		volumeTextIdxBeforeUpdateHooks = append(volumeTextIdxBeforeUpdateHooks, volumeTextIdxHook)
	case boil.BeforeDeleteHook:
		volumeTextIdxBeforeDeleteHooks = append(volumeTextIdxBeforeDeleteHooks, volumeTextIdxHook)
	case boil.BeforeUpsertHook:
		volumeTextIdxBeforeUpsertHooks = append(volumeTextIdxBeforeUpsertHooks, volumeTextIdxHook)
	case boil.AfterInsertHook:
		volumeTextIdxAfterInsertHooks = append(volumeTextIdxAfterInsertHooks, volumeTextIdxHook)
	case boil.AfterSelectHook:
		volumeTextIdxAfterSelectHooks = append(volumeTextIdxAfterSelectHooks, volumeTextIdxHook)
	case boil.AfterUpdateHook:
		volumeTextIdxAfterUpdateHooks = append(volumeTextIdxAfterUpdateHooks, volumeTextIdxHook)
	case boil.AfterDeleteHook:
		volumeTextIdxAfterDeleteHooks = append(volumeTextIdxAfterDeleteHooks, volumeTextIdxHook)
	case boil.AfterUpsertHook:
		volumeTextIdxAfterUpsertHooks = append(volumeTextIdxAfterUpsertHooks, volumeTextIdxHook)
	}
}

// OneP returns a single volumeTextIdx record from the query, and panics on error.
func (q volumeTextIdxQuery) OneP() *VolumeTextIdx {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single volumeTextIdx record from the query.
func (q volumeTextIdxQuery) One() (*VolumeTextIdx, error) {
	o := &VolumeTextIdx{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for volume_text_idx")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all VolumeTextIdx records from the query, and panics on error.
func (q volumeTextIdxQuery) AllP() VolumeTextIdxSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all VolumeTextIdx records from the query.
func (q volumeTextIdxQuery) All() (VolumeTextIdxSlice, error) {
	var o VolumeTextIdxSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to VolumeTextIdx slice")
	}

	if len(volumeTextIdxAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all VolumeTextIdx records in the query, and panics on error.
func (q volumeTextIdxQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all VolumeTextIdx records in the query.
func (q volumeTextIdxQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count volume_text_idx rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q volumeTextIdxQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q volumeTextIdxQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if volume_text_idx exists")
	}

	return count > 0, nil
}

// VolumeG pointed to by the foreign key.
func (o *VolumeTextIdx) VolumeG(mods ...qm.QueryMod) volumeQuery {
	return o.VolumeByFk(boil.GetDB(), mods...)
}

// Volume pointed to by the foreign key.
func (o *VolumeTextIdx) VolumeByFk(exec boil.Executor, mods ...qm.QueryMod) volumeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.Volume),
	}

	queryMods = append(queryMods, mods...)

	query := Volumes(exec, queryMods...)
	queries.SetFrom(query.Query, "\"volume\"")

	return query
}

// LoadVolume allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (volumeTextIdxL) LoadVolume(e boil.Executor, singular bool, maybeVolumeTextIdx interface{}) error {
	var slice []*VolumeTextIdx
	var object *VolumeTextIdx

	count := 1
	if singular {
		object = maybeVolumeTextIdx.(*VolumeTextIdx)
	} else {
		slice = *maybeVolumeTextIdx.(*VolumeTextIdxSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &volumeTextIdxR{}
		}
		args[0] = object.Volume
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &volumeTextIdxR{}
			}
			args[i] = obj.Volume
		}
	}

	query := fmt.Sprintf(
		"select * from \"volume\" where \"id\" in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Volume")
	}
	defer results.Close()

	var resultSlice []*Volume
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Volume")
	}

	if len(volumeTextIdxAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Volume = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Volume == foreign.ID {
				local.R.Volume = foreign
				break
			}
		}
	}

	return nil
}

// SetVolumeG of the volume_text_idx to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeTextIdx.
// Uses the global database handle.
func (o *VolumeTextIdx) SetVolumeG(insert bool, related *Volume) error {
	return o.SetVolume(boil.GetDB(), insert, related)
}

// SetVolumeP of the volume_text_idx to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeTextIdx.
// Panics on error.
func (o *VolumeTextIdx) SetVolumeP(exec boil.Executor, insert bool, related *Volume) {
	if err := o.SetVolume(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVolumeGP of the volume_text_idx to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeTextIdx.
// Uses the global database handle and panics on error.
func (o *VolumeTextIdx) SetVolumeGP(insert bool, related *Volume) {
	if err := o.SetVolume(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVolume of the volume_text_idx to the related item.
// Sets o.R.Volume to related.
// Adds o to related.R.VolumeTextIdx.
func (o *VolumeTextIdx) SetVolume(exec boil.Executor, insert bool, related *Volume) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"volume_text_idx\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"volume"}),
		strmangle.WhereClause("\"", "\"", 2, volumeTextIdxPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.Volume}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Volume = related.ID

	if o.R == nil {
		o.R = &volumeTextIdxR{
			Volume: related,
		}
	} else {
		o.R.Volume = related
	}

	if related.R == nil {
		related.R = &volumeR{
			VolumeTextIdx: o,
		}
	} else {
		related.R.VolumeTextIdx = o
	}

	return nil
}

// VolumeTextIdxesG retrieves all records.
func VolumeTextIdxesG(mods ...qm.QueryMod) volumeTextIdxQuery {
	return VolumeTextIdxes(boil.GetDB(), mods...)
}

// VolumeTextIdxes retrieves all the records using an executor.
func VolumeTextIdxes(exec boil.Executor, mods ...qm.QueryMod) volumeTextIdxQuery {
	mods = append(mods, qm.From("\"volume_text_idx\""))
	return volumeTextIdxQuery{NewQuery(exec, mods...)}
}

// FindVolumeTextIdxG retrieves a single record by ID.
func FindVolumeTextIdxG(volume int, selectCols ...string) (*VolumeTextIdx, error) {
	return FindVolumeTextIdx(boil.GetDB(), volume, selectCols...)
}

// FindVolumeTextIdxGP retrieves a single record by ID, and panics on error.
func FindVolumeTextIdxGP(volume int, selectCols ...string) *VolumeTextIdx {
	retobj, err := FindVolumeTextIdx(boil.GetDB(), volume, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindVolumeTextIdx retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVolumeTextIdx(exec boil.Executor, volume int, selectCols ...string) (*VolumeTextIdx, error) {
	volumeTextIdxObj := &VolumeTextIdx{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"volume_text_idx\" where \"volume\"=$1", sel,
	)

	q := queries.Raw(exec, query, volume)

	err := q.Bind(volumeTextIdxObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from volume_text_idx")
	}

	return volumeTextIdxObj, nil
}

// FindVolumeTextIdxP retrieves a single record by ID with an executor, and panics on error.
func FindVolumeTextIdxP(exec boil.Executor, volume int, selectCols ...string) *VolumeTextIdx {
	retobj, err := FindVolumeTextIdx(exec, volume, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *VolumeTextIdx) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *VolumeTextIdx) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *VolumeTextIdx) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *VolumeTextIdx) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no volume_text_idx provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(volumeTextIdxColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	volumeTextIdxInsertCacheMut.RLock()
	cache, cached := volumeTextIdxInsertCache[key]
	volumeTextIdxInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			volumeTextIdxColumns,
			volumeTextIdxColumnsWithDefault,
			volumeTextIdxColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(volumeTextIdxType, volumeTextIdxMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(volumeTextIdxType, volumeTextIdxMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"volume_text_idx\" (\"%s\") VALUES (%s)", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"volume_text_idx\" DEFAULT VALUES"
		}

		if len(cache.retMapping) != 0 {
			cache.query += fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into volume_text_idx")
	}

	if !cached {
		volumeTextIdxInsertCacheMut.Lock()
		volumeTextIdxInsertCache[key] = cache
		volumeTextIdxInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single VolumeTextIdx record. See Update for
// whitelist behavior description.
func (o *VolumeTextIdx) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single VolumeTextIdx record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *VolumeTextIdx) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the VolumeTextIdx, and panics on error.
// See Update for whitelist behavior description.
func (o *VolumeTextIdx) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the VolumeTextIdx.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *VolumeTextIdx) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	volumeTextIdxUpdateCacheMut.RLock()
	cache, cached := volumeTextIdxUpdateCache[key]
	volumeTextIdxUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(volumeTextIdxColumns, volumeTextIdxPrimaryKeyColumns, whitelist)
		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update volume_text_idx, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"volume_text_idx\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, volumeTextIdxPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(volumeTextIdxType, volumeTextIdxMapping, append(wl, volumeTextIdxPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update volume_text_idx row")
	}

	if !cached {
		volumeTextIdxUpdateCacheMut.Lock()
		volumeTextIdxUpdateCache[key] = cache
		volumeTextIdxUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q volumeTextIdxQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q volumeTextIdxQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for volume_text_idx")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o VolumeTextIdxSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o VolumeTextIdxSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o VolumeTextIdxSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VolumeTextIdxSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), volumeTextIdxPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"UPDATE \"volume_text_idx\" SET %s WHERE (\"volume\") IN (%s)",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(volumeTextIdxPrimaryKeyColumns), len(colNames)+1, len(volumeTextIdxPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in volumeTextIdx slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *VolumeTextIdx) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *VolumeTextIdx) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *VolumeTextIdx) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *VolumeTextIdx) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no volume_text_idx provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(volumeTextIdxColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	volumeTextIdxUpsertCacheMut.RLock()
	cache, cached := volumeTextIdxUpsertCache[key]
	volumeTextIdxUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			volumeTextIdxColumns,
			volumeTextIdxColumnsWithDefault,
			volumeTextIdxColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			volumeTextIdxColumns,
			volumeTextIdxPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert volume_text_idx, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(volumeTextIdxPrimaryKeyColumns))
			copy(conflict, volumeTextIdxPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"volume_text_idx\"", updateOnConflict, ret, update, conflict, whitelist)

		cache.valueMapping, err = queries.BindMapping(volumeTextIdxType, volumeTextIdxMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(volumeTextIdxType, volumeTextIdxMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert volume_text_idx")
	}

	if !cached {
		volumeTextIdxUpsertCacheMut.Lock()
		volumeTextIdxUpsertCache[key] = cache
		volumeTextIdxUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single VolumeTextIdx record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *VolumeTextIdx) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single VolumeTextIdx record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *VolumeTextIdx) DeleteG() error {
	if o == nil {
		return errors.New("models: no VolumeTextIdx provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single VolumeTextIdx record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *VolumeTextIdx) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single VolumeTextIdx record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VolumeTextIdx) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no VolumeTextIdx provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), volumeTextIdxPrimaryKeyMapping)
	sql := "DELETE FROM \"volume_text_idx\" WHERE \"volume\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from volume_text_idx")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q volumeTextIdxQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q volumeTextIdxQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no volumeTextIdxQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from volume_text_idx")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o VolumeTextIdxSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o VolumeTextIdxSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no VolumeTextIdx slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o VolumeTextIdxSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VolumeTextIdxSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no VolumeTextIdx slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(volumeTextIdxBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), volumeTextIdxPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"DELETE FROM \"volume_text_idx\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, volumeTextIdxPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(volumeTextIdxPrimaryKeyColumns), 1, len(volumeTextIdxPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from volumeTextIdx slice")
	}

	if len(volumeTextIdxAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *VolumeTextIdx) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *VolumeTextIdx) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *VolumeTextIdx) ReloadG() error {
	if o == nil {
		return errors.New("models: no VolumeTextIdx provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VolumeTextIdx) Reload(exec boil.Executor) error {
	ret, err := FindVolumeTextIdx(exec, o.Volume)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *VolumeTextIdxSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *VolumeTextIdxSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VolumeTextIdxSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty VolumeTextIdxSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VolumeTextIdxSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	volumeTextIdxes := VolumeTextIdxSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), volumeTextIdxPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"SELECT \"volume_text_idx\".* FROM \"volume_text_idx\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, volumeTextIdxPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(volumeTextIdxPrimaryKeyColumns), 1, len(volumeTextIdxPrimaryKeyColumns)),
	)

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&volumeTextIdxes)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in VolumeTextIdxSlice")
	}

	*o = volumeTextIdxes

	return nil
}

// VolumeTextIdxExists checks if the VolumeTextIdx row exists.
func VolumeTextIdxExists(exec boil.Executor, volume int) (bool, error) {
	var exists bool

	sql := "select exists(select 1 from \"volume_text_idx\" where \"volume\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, volume)
	}

	row := exec.QueryRow(sql, volume)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if volume_text_idx exists")
	}

	return exists, nil
}

// VolumeTextIdxExistsG checks if the VolumeTextIdx row exists.
func VolumeTextIdxExistsG(volume int) (bool, error) {
	return VolumeTextIdxExists(boil.GetDB(), volume)
}

// VolumeTextIdxExistsGP checks if the VolumeTextIdx row exists. Panics on error.
func VolumeTextIdxExistsGP(volume int) bool {
	e, err := VolumeTextIdxExists(boil.GetDB(), volume)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// VolumeTextIdxExistsP checks if the VolumeTextIdx row exists. Panics on error.
func VolumeTextIdxExistsP(exec boil.Executor, volume int) bool {
	e, err := VolumeTextIdxExists(exec, volume)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
