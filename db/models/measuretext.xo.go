// Package xo_models contains the types for schema 'public'.
package xo_models

// GENERATED BY XO. DO NOT EDIT.

import (
	"errors"
)

// MeasureText represents a row from '"public"."measure_text"'.
type MeasureText struct {
	Record int64  `json:"record"` // record
	Metric int64  `json:"metric"` // metric
	Datum  string `json:"datum"`  // datum

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the MeasureText exists in the database.
func (mt *MeasureText) Exists() bool {
	return mt._exists
}

// Deleted provides information if the MeasureText has been deleted from the database.
func (mt *MeasureText) Deleted() bool {
	return mt._deleted
}

// Insert inserts the MeasureText to the database.
func (mt *MeasureText) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if mt._exists {
		return errors.New("insert failed: already exists")
	}

	// sql insert query, primary key must be provided
	const sqlstr = `INSERT INTO "public"."measure_text" (` +
		`"record", "metric", "datum"` +
		`) VALUES (` +
		`$1, $2, $3` +
		`)`

	// run query
	XOLog(sqlstr, mt.Record, mt.Metric, mt.Datum)
	err = db.QueryRow(sqlstr, mt.Record, mt.Metric, mt.Datum).Scan(&mt.Metric)
	if err != nil {
		return err
	}

	// set existence
	mt._exists = true

	return nil
}

// Update updates the MeasureText in the database.
func (mt *MeasureText) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !mt._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if mt._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE "public"."measure_text" SET (` +
		`"record", "datum"` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE "metric" = $3`

	// run query
	XOLog(sqlstr, mt.Record, mt.Datum, mt.Metric)
	_, err = db.Exec(sqlstr, mt.Record, mt.Datum, mt.Metric)
	return err
}

// Save saves the MeasureText to the database.
func (mt *MeasureText) Save(db XODB) error {
	if mt.Exists() {
		return mt.Update(db)
	}

	return mt.Insert(db)
}

// Upsert performs an upsert for MeasureText.
//
// NOTE: PostgreSQL 9.5+ only
func (mt *MeasureText) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if mt._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO "public"."measure_text" (` +
		`"record", "metric", "datum"` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT ("metric") DO UPDATE SET (` +
		`"record", "metric", "datum"` +
		`) = (` +
		`EXCLUDED."record", EXCLUDED."metric", EXCLUDED."datum"` +
		`)`

	// run query
	XOLog(sqlstr, mt.Record, mt.Metric, mt.Datum)
	_, err = db.Exec(sqlstr, mt.Record, mt.Metric, mt.Datum)
	if err != nil {
		return err
	}

	// set existence
	mt._exists = true

	return nil
}

// Delete deletes the MeasureText from the database.
func (mt *MeasureText) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !mt._exists {
		return nil
	}

	// if deleted, bail
	if mt._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM "public"."measure_text" WHERE "metric" = $1`

	// run query
	XOLog(sqlstr, mt.Metric)
	_, err = db.Exec(sqlstr, mt.Metric)
	if err != nil {
		return err
	}

	// set deleted
	mt._deleted = true

	return nil
}

// Metric returns the Metric associated with the MeasureText's Metric (metric).
//
// Generated from foreign key 'measure_text_metric_fkey'.
func (mt *MeasureText) MetricByMetricID(db XODB) (*Metric, error) {
	return MetricByID(db, mt.Metric)
}

// Record returns the Record associated with the MeasureText's Record (record).
//
// Generated from foreign key 'measure_text_record_fkey'.
func (mt *MeasureText) RecordByRecordID(db XODB) (*Record, error) {
	return RecordByID(db, mt.Record)
}

// MeasureTextByRecordMetric retrieves a row from '"public"."measure_text"' as a MeasureText.
//
// Generated from index 'measure_text_pkey'.
func MeasureTextByRecordMetric(db XODB, record int64, metric int64) (*MeasureText, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`"record", "metric", "datum" ` +
		`FROM "public"."measure_text" ` +
		`WHERE "record" = $1 AND "metric" = $2`

	// run query
	XOLog(sqlstr, record, metric)
	mt := MeasureText{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, record, metric).Scan(&mt.Record, &mt.Metric, &mt.Datum)
	if err != nil {
		return nil, err
	}

	return &mt, nil
}
